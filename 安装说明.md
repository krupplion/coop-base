# ORG_FULL_NAME协同录入系统 · 安装说明

局域网协同录入系统：财务部一台电脑作为本地服务器，其余人员通过浏览器/客户端连接，输入「配网数字」即可自动联网，无需手动配置网络参数。

---

## 一、系统环境要求

| 项目 | 要求 |
|---|---|
| 操作系统 | Windows 10 / Windows 11（64 位） |
| 硬件 | 普通办公电脑即可；建议服务器（财务电脑）≥ 4GB 内存 |
| 网络 | 与协同人员处于同一局域网（同一路由器/网段） |
| 依赖 | **无需预装 Python**；安装包已内嵌运行环境 |
| 磁盘空间 | 安装目录约 120 MB；用户数据另存于 `%LOCALAPPDATA%\APP_DATA_NAME` |
| 权限 | 安装到 `C:\Program Files` 需要管理员权限（UAC 弹窗请允许）；数据目录无需特殊权限 |
| 浏览器 | 任意现代浏览器（Edge / Chrome / Firefox 均可），用于访问 Web 界面 |

> 说明：本系统为**纯局域网本地应用**，数据不上云。服务器电脑关机期间协同人员无法录入。

---

## 二、安装步骤

1. 双击 `setup_coop.exe`（安装程序）。
2. 若出现用户账户控制（UAC）提示，点击「是」允许安装。
3. 阅读欢迎界面，点击「下一步」。
4. **选择安装路径**（默认 `C:\Program Files\APP_DATA_NAME`，可改成其他盘符/路径）。
5. 选择附加组件：
   - ☑ 主程序（必选，不可取消；含桌面与开始菜单快捷方式）
   - ☑ 防火墙放行（**财务电脑建议保持勾选**；自动放行 TCP 8100 与 UDP 39997，省去手动配置）
   - ☐ 开机自动启动服务器（**财务电脑建议勾选**，确保协同录入随时可用）
6. 点击「安装」，等待进度完成。
7. 安装结束可选择「启动服务器」，点击「完成」。

安装完成后生成：
- 开始菜单 / 桌面：「APP_DATA_NAME - 启动服务器」「APP_DATA_NAME - 连接服务器」
- 安装目录下的 `server\`（服务器）、`client\`（客户端连接工具）、`安装说明.md`

---

## 三、使用步骤

### 财务电脑（服务器）
1. 运行「启动服务器」。会弹出黑色控制台窗口，显示：
   - 本机访问地址：`http://127.0.0.1:8100`
   - **配网数字**（如 `481927`，每次启动随机生成）
   - 默认账号：`admin / 123456`
2. 浏览器自动打开登录页；用 `admin / 123456` 登录，**请立即在「权限管理 / 系统设置」中修改默认密码**。
3. 把「配网数字」告知需要协同的同事。

### 其余协同人员（客户端）
1. 运行「连接服务器」（控制台窗口）。
2. 输入财务电脑显示的**配网数字**，回车。
3. 程序自动在局域网内发现服务器并打开浏览器登录页；输入各自账号即可录入。
   - 若提示「未找到服务器」，请确认：①财务电脑服务器已启动；②配网数字一致；③防火墙已放行 UDP 39997。

---

## 四、⚠️ 需用户自行配置的参数（重要）

安装后以下参数**不会自动填好**，需由贵所自行配置：

| 参数 | 默认值 | 何处配置 | 说明 |
|---|---|---|---|
| **管理员密码** | `123456` | 登录后「权限管理 / 系统设置」 | **必须修改**，否则存在安全风险 |
| **AI 发票识别 API** | 空（未启用） | 「系统设置」→ AI 接口 | 需贵所自备**兼容 OpenAI 格式的多模态 API**（地址 + API Key + 模型名）；不配置则 AI 发票识别不可用，其余功能不受影响 |
| **配网数字** | 每次启动随机生成 | 服务器控制台自动显示 | 无需配置，但需告知同事以完成连接 |
| **服务端口** | `8100` | 高级：设置环境变量 `COOP_PORT` | 默认即可；如被占用可在服务器启动前设置 `COOP_PORT=9000` 等 |
| **数据存储位置** | `%LOCALAPPDATA%\APP_DATA_NAME` | 高级：设置环境变量 `COOP_DATA` | 默认即可；如想指定数据盘可设置该变量指向其他目录 |

> AI 发票识别：系统支持接入 DeepSeek / 通义千问 / OpenAI 等任意兼容 OpenAI 格式的多模态模型，用于在「发票识别」模块自动识别电子发票并归集费用科目。请提供有效的 API 地址与密钥。

---

## 五、防火墙配置（如无法连接）

服务器需允许入站连接：
- **TCP 8100**（Web 服务）
- **UDP 39997**（局域网自动配网发现）

安装时若勾选了「防火墙放行」组件，安装程序会自动创建上述两条入站规则（规则名 `CoopBase-Web-TCP8100`、`CoopBase-Discover-UDP39997`），**无需手动配置**；卸载时也会一并清除。

如安装时未勾选、或使用了第三方安全软件，可手动放行：在「Windows Defender 防火墙 → 高级设置 → 入站规则」中新建规则放行上述端口，或以管理员身份运行：
```
netsh advfirewall firewall add rule name="CoopBase-Web-TCP8100" dir=in action=allow protocol=TCP localport=8100
netsh advfirewall firewall add rule name="CoopBase-Discover-UDP39997" dir=in action=allow protocol=UDP localport=39997
```

---

## 六、卸载

1. 「控制面板 → 程序和功能」找到「ORG_FULL_NAME协同录入系统」→ 卸载；
   或开始菜单「APP_DATA_NAME → Uninstall」。
2. 卸载会移除程序与快捷方式，**保留业务数据**（`%LOCALAPPDATA%\APP_DATA_NAME`）。
3. 如需彻底清空数据，卸载后手动删除 `%LOCALAPPDATA%\APP_DATA_NAME` 文件夹。

---

## 七、常见问题

- **浏览器打不开 / 连接失败**：确认财务电脑服务器 console 窗口仍在运行；客户端输入的配网数字是否与服务器显示一致。
- **换了一台财务电脑当服务器**：原数据在旧电脑的 `%LOCALAPPDATA%\APP_DATA_NAME`，如需迁移请复制该目录到新电脑同名位置。
- **端口被占用**：服务器启动前设置环境变量 `COOP_PORT` 为其他端口，并在防火墙放行该端口。
