# 交付说明 · 局域网协同录入系统

## 已完成
- **完整可运行系统**：Flask + SQLite，已通过 15 项接口冒烟测试（登录、RBAC、字段级权限、案号生成、并发冲突合并、薪酬公式、Word 结算单批量生成、Excel 导入去重/导出、审计日志、UDP 配网发现）。
- **局域网自动配网**：客户端输入 6 位配网数字，UDP 广播自动发现服务器并打开浏览器，无需手动配置 IP/端口。
- **权限架构**：财务经理(admin) / 财务专员(finance) / 案件专员(case) / 查看(viewer) 四角色，服务端强制校验 + 案件表字段级权限（财务字段仅财务角色可写）。
- **协同与冲突**：SQLite WAL + 乐观锁版本号 + 字段级合并；冲突字段「最后写入优先」且被覆盖旧值写入日志可追溯。
- **操作日志**：全量增删改查/登录/导入导出审计（用户、时间、动作、对象、详情、IP）。
- **六大模块**：案件管理（自动案号，如 #260901）、薪酬计算（沙箱公式 + 固定金额）、批量 Word 结算单（支持 {{占位符}} 自定义模板）、Excel 导入清洗/导出、权限管理、AI 发票识别（兼容 OpenAI 格式 API，自动归集费用科目）。

## 文件清单（coop-base/）
- `app.py` 主服务（约 700 行：数据模型、RBAC、全部 API、业务逻辑）
- `discovery.py` 配网发现响应器 / `client_connect.py` 客户端配网工具
- `templates/index.html` 前端单页应用（金蝶简洁风格，零外部依赖）
- `start_server.bat` / `client_connect.bat` 一键启动脚本
- `README.md` 完整部署文档 / `requirements.txt`

## 使用方式
1. 财务部电脑双击 `start_server.bat`（端口 8100，配网数字启动时显示）
2. 其他 3 台电脑运行 `client_connect.bat`，输入配网数字即自动连接
3. 默认账号 admin / 123456，登录后立即修改

## 测试结果摘要
- 冲突合并：过期版本写入返回 `conflict_merged:true` 并记录覆盖明细 ✓
- 字段级权限：案件专员写财务字段被拒、删案件被拒 ✓
- 薪酬试算：王五 60000×10%+45000×2%=6900 元，计算正确 ✓
- 结算单：1 案件 × 2 人员生成 2 份 docx 并打包 ZIP ✓
- 导入回环：重复数据自动跳过 ✓  配网发现：UDP 应答返回正确 IP:端口 ✓

## 注意事项
- 依赖安装在国内网络下建议用清华镜像：`pip install -i https://pypi.tuna.tsinghua.edu.cn/simple -r requirements.txt`
- 防火墙需放行 TCP 8100 与 UDP 39997
- AI 发票识别需在系统设置中配置兼容 OpenAI 格式的多模态 API（地址 + 密钥 + 模型）
- 代码可正常接入 GitHub / Gitee 等仓库（README 第七节有命令）

## 二次迭代：登录问题修复（2026-08-27）
- **根因**：前端 `api()` 把所有 401 当"会话过期"处理并抛出异常，`doLogin` 的 catch 静默吞掉 → 密码错误时界面无反应，表现为"无法登录"。后端验证逻辑本身正常。
- **修复**：前端请求封装增加 raw 模式与网络异常友好提示；登录卡片增加常驻错误提示区；后端细分错误提示（账户不存在/已停用/密码错误/参数缺失）；全局异常均返回 JSON；登录失败写入审计日志。
- **访问方式提醒**：必须通过 `http://服务器IP:8100` 访问系统，直接双击打开 HTML 文件无法连接后端。

## 三次迭代：功能模块增强（2026-08-28）
本次新增 5 大功能模块，均与现有金蝶简洁风格保持一致：

1. **结算自定义模板界面**：可视化配置结算单模板——可设置模板名称、标题、显示字段、字段顺序（上移/下移）、是否显示批次号/日期/结算人员、落款文字；支持保存、编辑、删除、设为默认、复用。生成结算单时下拉选择模板，按 JSON 模板渲染 Word（无需外部 docx 文件）。
2. **发票 OCR 本地离线识别**：集成开源 **RapidOCR**（ONNX Runtime + PP-OCRv3，模型随包内置），识别发票号码、销售方、价税合计、税额、开票日期，并按品名关键词自动归集费用科目。**全程数据不出本机**；未安装依赖时前端一键「自动配置依赖」（服务器端 pip 安装，含清华镜像）。接口：`/api/ocr/status`、`/api/ocr/setup`、`/api/invoices/ocr-recognize`。
3. **历史数据导入增强**：新增「导入模板」——自定义「文件列名 → 系统字段」映射并保存，支持案件/花名册/客户三类实体，后续重复导入直接选用（`template_id`），无需重复配置。
4. **数据录入管理扩展**：新增**人员花名册**、**客户信息**两个模块，均支持手动录入 + Excel/CSV 批量导入；导入自动校验必填项、数字/日期格式、完整性，重复（姓名+工号 / 客户名称）自动跳过，并逐行返回错误明细。
5. **功能卡片**：工作台新增统一功能导航卡片，展示各模块名称、用途说明与操作入口，一键直达，按权限自动过滤。

- 后端新增数据表：`settle_templates`（结算模板）、`import_templates`（导入模板）、`staff`（花名册）、`customers`（客户）。
- 新增依赖：`rapidocr_onnxruntime>=1.2`（本地 OCR）。
- 测试验证：结算模板渲染、OCR 识别（发票号/金额/税额/科目归集）、导入模板映射、花名册/客户 CRUD 与导入校验（错误明细精确到行）全部通过。
