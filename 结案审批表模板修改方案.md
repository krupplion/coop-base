# 《ORG_NAME案件结案审批表》模板修改方案

依据纸质表单（图：`(ORG_NAME) 案件结案审批表`）对现有协同录入系统的结算单模板进行改造，新增「案件结案审批表」模板类型，用于案件办结归档、收益核算与提成计算。

---

## 1. 结构变更

### 1.1 新增模板类型
| 类型 | 用途 | 与现有薪酬结算单的关系 |
|---|---|---|
| 薪酬结算单（原） | 按人员逐案生成薪酬/提成发放单据 | 保留，复用现有逻辑 |
| 案件结案审批表（新） | 案件办结归档、费用收回、收益计算、提成审批 | 新增默认审批表模板 |

### 1.2 新增数据表
- `case_extra_fields`（案件扩展字段，避免案件表字段过多）：key/value 模式。
  - plaintiff（原告）、defendant（被告）、case_cause（案由）、project（项目）、subject_amount（标的）、fee_standard（收费标准）、expected_profit（预期收益）、section（板块）、maintain_user（维护人）、volume_no（卷号）。
- `case_fee_recoveries`（费用收回明细）：recovery_date、item、amount、confirm。
- `case_commissions`（提成明细）：person、role、rate、base_amount、amount、calc_process。

### 1.3 页面层级调整
- 案件管理弹窗：新增「案件扩展信息」分组（原告/被告/案由/标的/项目/收费标准/预期收益/维护人/卷号）。
- 案件管理页面：新增「费用收回明细」「收益与提成」子卡片。
- 结算单页面：新增「生成案件结案审批表」按钮，与「批量生成薪酬结算单」并列。

---

## 2. 样式变更

### 2.1 配色
| 用途 | 色值 | 说明 |
|---|---|---|
| 主色（标题/表头） | #1e6fd9 | 沿用现有蓝色，保持品牌一致性 |
| 表头底色 | #f7f9fc | 浅灰蓝，模拟纸质表单表头 |
| 边框 | #d0d5dd | 略深于现有，打印更清晰 |
| 金额红色 | #e6432d | 金额/提成高亮 |
| 计算过程灰 | #5a6478 | 公式与中间步骤用浅灰文字 |

### 2.2 排版
- 标题：黑体、18pt、居中。
- 表头：12pt、加粗、表头行背景色填充。
- 正文：11pt。
- 间距：段前 3pt、段后 3pt；表格单元格上下 4pt。
- 圆角：审批表卡片不用圆角，保持正式公文风格；页面内按钮保留 4px 圆角。
- 阴影：审批表 Word 导出无阴影；页面卡片保留现有浅阴影。
- 图标：结案审批表入口使用「文件审批」图标（svg）。

### 2.3 Word 导出样式
- A4 竖版，页边距 2.0cm。
- 标题居中，表格外框实线 1pt，内部横线 0.5pt。
- 收益计算过程、提成计算过程使用独立单元格，公式左对齐，结果右对齐。

---

## 3. 字段变更

### 3.1 新增字段（案件扩展信息）
| 字段名 | 数据类型 | 校验规则 | 中文名 |
|---|---|---|---|
| plaintiff | TEXT | 非必填 | 原告 |
| defendant | TEXT | 非必填 | 被告 |
| section | TEXT | 非必填 | 板块 |
| case_cause | TEXT | 非必填 | 案由 |
| subject_amount | REAL | ≥0 | 标的 |
| project | TEXT | 非必填 | 项目 |
| fee_standard | TEXT | 非必填 | 收费标准 |
| expected_profit | REAL | ≥0 | 预期收益 |
| maintain_user | TEXT | 非必填 | 维护人 |
| volume_no | TEXT | 非必填 | 卷号 |

### 3.2 费用收回明细字段
| 字段名 | 数据类型 | 校验规则 |
|---|---|---|
| recovery_date | TEXT | 日期格式 YYYY-MM-DD |
| item | TEXT | 必填，如"担保费/减半诉讼费" |
| amount | REAL | 必填，数字 |

### 3.3 提成明细字段
| 字段名 | 数据类型 | 校验规则 |
|---|---|---|
| person | TEXT | 必填 |
| role | TEXT | 必填（承办人/维护人/其他） |
| rate | REAL | ≥0，百分比，如 0.05 |
| base_amount | REAL | 提成基数 |
| amount | REAL | 提成金额 = base_amount × rate |
| calc_process | TEXT | 完整计算过程字符串 |

### 3.4 重命名/复用
- 案件表 `owner` → 审批表「承办人」。
- 案件表 `case_no` → 审批表「卷号」。
- 案件表 `agent_fee` → 审批表「代理费/实际收益」。

---

## 4. 布局变更

### 4.1 纸质表单映射
```
┌───────────────────────────────────────────────┐
│       (ORG_NAME) 案件结案审批表                   │
│  维护人：____  承办人：____  卷号：____        │
├───────────────────────────────────────────────┤
│ 案件基本信息                                  │
│ ┌───────────┬────────────┬──────────┬────────┐│
│ │ 原告      │            │ 被告     │        ││
│ │ 板块      │            │ 案由     │        ││
│ │ 标的      │            │ 项目     │        ││
│ │ 收费标准  │            │ 预期收益 │        ││
│ └───────────┴────────────┴──────────┴────────┘│
├───────────────────────────────────────────────┤
│ 案件办结情况                                  │
│ 1. 费用收回：                                  │
│    日期        款项说明            金额       │
│    2026-08-21  垫付担保费          636.00     │
│    ...                                        │
│    出纳审核意见：____                         │
│                                               │
│ 2. 收益：                                      │
│    21072 + 34391.49 - 636 - 4079 - ...        │
│    = 34391.49 元                              │
│                                               │
│ 3. 备注：____                                 │
│                                               │
│ 4. 提成：                                      │
│    承办人 刘莹  5%  34391×5%=1720 元          │
│    维护人 占绪奎 8%  34391×8%=2751 元         │
├───────────────────────────────────────────────┤
│ 维护人签字：____   日期：____年__月__日       │
│ 财务主管：____     日期：____年__月__日       │
│ 执行主任：____     日期：____年__月__日       │
├───────────────────────────────────────────────┤
│ 说明：本表是案件办结归档的凭证...            │
└───────────────────────────────────────────────┘
```

### 4.2 页面栅格
- 案件编辑弹窗：两列栅格，`grid-template-columns: repeat(2, 1fr)`。
- 费用收回明细表格：3 列（日期/款项/金额）。
- 提成明细表格：5 列（角色/人员/比例/计算过程/金额）。
- 响应式断点：
  - ≥768px：两列案件信息。
  - <768px：单列堆叠。

### 4.3 对齐规则
- 金额列：右对齐，千分位 + 2 位小数。
- 文本列：左对齐。
- 表头：左对齐/居中（金额列居中）。
- 收益计算公式：左对齐，等号与结果行居中。

---

## 5. 功能变更

### 5.1 新增功能
- **案件扩展信息录入**：在案件弹窗新增「扩展信息」分组。
- **费用收回明细维护**：每案可维护多笔回款/费用，支持新增、删除。
- **收益自动计算**：根据「总收款 − 各项费用」自动计算，展示完整公式。
- **提成明细自动计算**：按人员/角色/比例自动计算提成金额，并**生成可读的计算过程字符串**。
- **结案审批表生成**：一键生成 Word 审批表，支持打印/归档。

### 5.2 增强功能
- 现有薪酬规则与审批表提成互通：已配置的薪酬规则可作为提成比例来源，审批表生成的提成数据可反向校验薪酬计算。
- 结算模板设计器：新增「审批表模板」类型，可配置是否显示费用明细/收益过程/提成过程。

### 5.3 移除功能
- 审批表模板不生成 ZIP 打包，单案单表输出 docx。

---

## 6. 设计规范

### 6.1 品牌色彩
| 名称 | 色值 | 用途 |
|---|---|---|
| ORG_NAME蓝 | #1e6fd9 | 标题、表头、主按钮、链接 |
| ORG_NAME深蓝 | #1559b3 | 悬停/按下态 |
| 金额红 | #e6432d | 金额、提成、重要提示 |
| 成功绿 | #18a058 | 已通过/已办结状态 |
| 正文黑 | #2b3445 | 正文文字 |
| 辅助灰 | #5a6478 | 计算过程、说明文字 |
| 边框灰 | #d0d5dd | 表格边框、分隔线 |
| 背景灰 | #f3f5f9 | 页面背景 |
| 表头浅蓝 | #eaf2fd | 表头背景 |

### 6.2 字体
- 页面 UI：`-apple-system, "Microsoft YaHei", "PingFang SC", sans-serif`
- Word 导出："宋体" / "Microsoft YaHei"，标题使用黑体。

### 6.3 字号层级
| 层级 | 字号 | 字重 | 用途 |
|---|---|---|---|
| H1 | 18pt | Bold | 审批表标题 |
| H2 | 14pt | Bold | 分组标题（案件基本信息/办结情况） |
| Body | 11pt | Normal | 正文、表格内容 |
| Caption | 10pt | Normal | 说明文字、计算过程 |

---

## 7. 目标效果

修改完成后：
1. 打开「结算单」页面，可切换「薪酬结算单」或「案件结案审批表」两种生成模式。
2. 在案件编辑弹窗中填写原告/被告/案由/标的/项目/收费标准/预期收益/维护人/卷号。
3. 在案件详情或结算单页面录入费用收回明细（多行）。
4. 系统自动计算「收益 = 收款合计 − 费用合计」，并以可读公式展示。
5. 系统自动按人员/比例计算提成，每行展示「基数 × 比例 = 金额」的完整过程。
6. 点击「生成案件结案审批表」得到与纸质表单版式一致的 Word 文档，可直接打印、签字、归档。
7. 原有「薪酬结算单」功能保持不变。

---

## 8. 验收标准

| 编号 | 验收项 | 检查方法 | 通过标准 |
|---|---|---|---|
| A1 | 案件扩展字段可录入 | 打开案件编辑弹窗 | 原告/被告/案由/标的/项目/收费标准/预期收益/维护人/卷号 9 个字段均可见且可保存 |
| A2 | 费用收回明细维护 | 案件详情页新增/删除费用行 | 可保存多行，删除后列表正确刷新，金额校验通过 |
| A3 | 收益自动计算 | 录入费用后查看收益卡片 | 公式为「收款合计 − 费用合计」，结果与手工计算一致 |
| A4 | 提成计算过程展示 | 录入提成规则后生成审批表 | Word 中每行提成显示「基数 × 比例 = 金额」的完整算式 |
| A5 | 审批表 Word 导出 | 生成审批表 docx | 文件可正常打开，版式与纸质表单基本一致，字段内容正确 |
| A6 | 原有薪酬结算单 | 勾选案件生成薪酬结算单 | 原有功能正常，ZIP 下载正确 |
| A7 | 权限控制 | 用案件专员/财务专员/财务经理分别登录 | 案件专员可见并编辑基本字段；财务角色可见财务字段；viewer 只读 |
| A8 | 响应式布局 | 浏览器窗口缩至 768px 以下 | 案件信息表单项单列堆叠，表格横向滚动不溢出 |
| A9 | 打印友好 | 用浏览器打印预览查看审批表页面 | 隐藏按钮/输入框，只保留审批表主体内容 |
| A10 | 数据完整性 | 生成审批表后删除费用明细重新生成 | 新审批表数据与最新费用明细一致 |

---

## 9. 特别要求：提成金额完整计算过程

### 9.1 计算依据
提成金额以「本案实际收益（代理费到账金额）」为基数，乘以该人员在薪酬规则中配置的比例。

### 9.2 公式与中间步骤
以纸质表单为例：

```
本案实际收益 = 实际到账总额 − 各项垫付费用
             = (21072 + 34391.49) − (636 + 4079 + 3170 + 4079 + 4354 + 4354)
             = 55463.49 − 20672
             = 34391.49 元

承办人刘莹提成：
  基数 × 比例 = 34391.49 × 5%
              = 1719.57 元
              ≈ 1720 元（四舍五入）

维护人占绪奎提成：
  基数 × 比例 = 34391.49 × 8%
              = 2751.32 元
              ≈ 2751 元（四舍五入）
```

### 9.3 展示要求
审批表「提成」区域每行必须展示：
1. 人员角色（承办人/维护人/其他）
2. 姓名
3. 提成比例
4. 计算算式：基数 × 比例 = **精确值 → 取整后金额**
5. 结果金额（**四舍五入取整**，取整时在金额旁标注「（取整）」）
6. 备注列（取整时显示「（取整）」，金额本身为整数时留空）

示例 Word 输出：
| 角色 | 姓名 | 比例 | 计算过程 | 金额（¥） | 备注 |
|---|---|---|---|---|---|
| 承办人 | 刘莹 | 5% | 34391.49 × 5% = 1719.57 → 取整 1720.00 元 | 1720.00（取整） | （取整） |
| 维护人 | 占绪奎 | 8% | 34391.49 × 8% = 2751.32 → 取整 2751.00 元 | 2751.00（取整） | （取整） |
| 参与人 | 王五 | 2% | 40000.00 × 2% = 800.00 元 | 800.00 | （空） |

### 9.3.1 取整规则（已实现）
- **取整时机**：批量生成模板时，提成金额自动执行**四舍五入取整**。
- **舍入方式**：采用财务习惯的 `ROUND_HALF_UP`（`.5` 一律进位），**不使用** Python 默认的「银行家舍入」（`.5` 取偶，会导致 1720.5→1720 的错误进位）。
- **精确值留痕**：取整前的精确金额存入 `case_commissions.raw_amount`（如 1719.57），取整后金额存 `amount`（如 1720.00），两者同时保留以便复核。
- **取整标注**：仅当「精确值 ≠ 取整值」时才在金额旁与备注列标注「（取整）」；金额本身为整数时不标注，避免误导。
- **计算过程**：`基数 × 比例 = 精确值 → 取整 金额 元`，完整链条可追溯。
- **适用范围**：手动新增提成 与 按薪酬规则自动计算提成 两条路径均执行同一取整逻辑。

### 9.4 可追溯性
- 计算过程字符串保存到 `case_commissions.calc_process`。
- 审批表 Word 中展示完整算式。
- 操作日志记录提成生成事件（含计算过程摘要）。

---

## 10. 实施说明（已实现）

### 10.1 已落地内容
| 项 | 实现 |
|---|---|
| 案件扩展字段 | cases 表新增 10 列；并通过 `migrate_db()` 自动为**已有旧库**补齐（不丢数据） |
| 费用收回 | `fee_recoveries` 表 + 增/查/删接口，金额正数=收入、负数=垫付 |
| 提成明细 | `case_commissions` 表 + 手动新增/自动计算/删除，`calc_process` 存完整算式 |
| 审批表渲染 | `render_close_approval_docx()`，按纸质版式输出 Word |
| 前端入口 | 结算单页面「案件结案审批表」卡片；案件弹窗新增 10 个扩展字段 |
| 权限 | 扩展文本字段受 `case.edit` 控制；标的/预期收益等金额字段受 `case.finance` 控制 |

### 10.2 新增接口
```
GET    /api/cases/<id>/recoveries            费用收回列表
POST   /api/cases/<id>/recoveries            新增费用收回
DELETE /api/cases/<id>/recoveries/<rid>      删除费用收回
GET    /api/cases/<id>/commissions           提成明细列表
POST   /api/cases/<id>/commissions           手动新增提成（自动算金额与过程）
POST   /api/cases/<id>/commissions/calc      按薪酬规则自动重算提成
DELETE /api/cases/<id>/commissions/<cmid>    删除提成
GET    /api/cases/<id>/close-approval        生成《案件结案审批表》Word
```

### 10.3 实测结果（以纸质表单数据验证）
- 案件扩展信息：原告/被告/板块/案由/标的 457244.51/项目/收费标准/预期收益 22862 全部正确写入审批表。
- 费用收回：8 笔（收入 21072 + 34391.49，垫付 636/4079/3170/4079/4354/4354）。
- 收益计算：`本案收益 = 55463.49 - 20672.00 = 34791.49 元`，与手工核算一致。
- 提成明细：`34391.49 × 5% = 1719.57 元`（承办人刘莹）、`34391.49 × 8% = 2751.32 元`（维护人占绪奎），与纸质表单 1720 / 2751 一致（纸质为四舍五入取整）。
- Word 版式：标题、维护人/承办人/卷号、4×4 基本信息表、费用收回表、收益公式、提成表（含算式）、签字栏、说明，与纸质表单一致。
