# 律师事务所业务管理系统 功能规格说明书

| 项目 | 内容 |
|---|---|
| 文档版本 | v1.0 |
| 编制日期 | 2026-08-28 |
| 适用范围 | 案件管理模块、薪酬计算模块、结算与文档生成模块 |
| 文档性质 | 功能需求规格说明（可执行实施方案） |
| 基础系统 | 现状系统（admin/123456，本地部署，57 个 API 路由） |

---

## 0. 需求总览

本规格说明围绕三项业务目标，对现状系统进行优化完善：

1. **案件管理**：提升新建案件窗口的信息展示能力（拖拽调宽、自动换行）；案件类型按民事案件分类规范化；字段命名与单位规范化。
2. **薪酬计算**：页面按「待结算 / 已结算」双区域组织，待结算案件完整展示计算公式所需全部信息。
3. **结算与文档生成**：计算完成后数据自动转入结算页，支持按自定义模板或默认模板批量生成结算文档。

需求与现状系统的对应关系：

| # | 需求 | 现状 | 改动性质 |
|---|---|---|---|
| 1.1 | 案件窗口可拖拽调宽 + 长文本自动换行 | 弹窗固定宽度、无换行策略 | 前端交互增强 |
| 1.2 | 案件类型按民事分类下拉 | 自由文本输入 | 前端表单改造 |
| 1.3 | "板块"更名"部门" | 字段标签"板块" | 前端文案调整（数据兼容） |
| 1.4 | 收费标准附加单位"人民币元" | 无单位提示 | 前端展示增强 |
| 2.1 | 薪酬页双区域（待结算/已结算） | 单区域渲染全部案件 | 前端重构 + 后端扩展 |
| 2.2 | 待结算案件完整展示计算要素 | 已展示大部分，缺结算状态归集 | 前端完善 |
| 3.1 | 计算后自动转入结算页 | 结算页独立勾选 | 前后端联动 |
| 3.2 | 模板批量生成结算文档 | 已支持（默认+自定义模板） | 维持并完善 |

---

## 1. 案件管理模块

### 1.1 新建案件窗口：拖拽调整宽度 + 长文本自动换行

**现状**：案件弹窗（`#caseMask`）固定宽度（约 720px），三模块 Tab 结构，字段内容超长时溢出或截断。

**目标规格**：

| 编号 | 规格项 | 说明 |
|---|---|---|
| 1.1.1 | 拖拽调宽 | 弹窗右下角提供拖拽手柄（resize 手柄），用户可水平/垂直拖动调整窗口大小 |
| 1.1.2 | 宽度范围 | 最小宽度 720px，最大宽度为视口宽度 90%；高度最小 480px、最大视口 90% |
| 1.1.3 | 尺寸记忆 | 关闭弹窗后记住本次尺寸（localStorage），下次打开沿用；用户可随时拖回默认 |
| 1.1.4 | 自动换行 | 弹窗内所有输入框、下拉框、文本域及结果表格启用 `word-break: break-word` / `white-space: normal`，长字段（项目名称、客户名称、收费标准、备注）自动换行完整展示，不截断、不横向溢出 |
| 1.1.5 | 布局自适应 | 拖宽后表单保持两列栅格布局并随宽度扩展列间距；超过 1200px 自动切换为三列 |
| 1.1.6 | 兼容性 | 拖拽不破坏三模块 Tab 切换、成员管理、保存逻辑；移动端/窄屏回退为固定宽度 |

**验收标准**：
- [ ] 新建/编辑案件弹窗可拖拽调整宽高，拖动平滑无卡顿；
- [ ] 关闭后重新打开，尺寸保持上次拖拽结果；
- [ ] 输入超长项目名称（>50 字）保存后重新打开，内容完整换行显示，无截断；
- [ ] 拖宽到 1200px 以上表单自适应三列，布局不塌陷。

### 1.2 案件类型：民事案件分类下拉

**现状**：`案件类型` 为自由文本输入框（`<input id="cType">`）。

**目标规格**：改为下拉选择，选项按民事案件常见案由分类设置：

| 分组 | 下拉选项 |
|---|---|
| 婚姻家庭 | 离婚纠纷、离婚后财产纠纷、抚养权纠纷、赡养纠纷、继承纠纷 |
| 合同纠纷 | 买卖合同纠纷、借款合同纠纷、租赁合同纠纷、建设工程合同纠纷、服务合同纠纷、其他合同纠纷 |
| 侵权 | 机动车交通事故责任纠纷、人身损害赔偿、财产损害赔偿、医疗损害责任纠纷 |
| 劳动争议 | 劳动争议、劳动合同纠纷、工伤赔偿纠纷 |
| 物权 | 房屋买卖合同纠纷、共有纠纷、相邻关系纠纷 |
| 其他 | 民间借贷纠纷、票据纠纷、执行异议、其他民事案件 |

**交互规格**：
1. 下拉框默认选中"请选择案件类型（可手动输入）"；
2. 允许用户**手动输入下拉列表之外的案由**（下拉框启用 `datalist` 或"其他"选项 + 补充输入框），保证业务灵活性；
3. 已保存的历史案件类型原样展示（数据库不迁移、不强制映射），仅新建/编辑时提供下拉；
4. 案件列表、数据信息库、结案审批表等处的类型展示不受影响。

**验收标准**：
- [ ] 新建案件时案件类型为下拉，包含上述全部民事分类选项；
- [ ] 可选择"其他"并手动录入自定义案由；
- [ ] 历史案件（如"买卖合同纠纷"为文本存储）编辑保存后不丢失、不报错。

### 1.3 "板块"字段更名"部门"

**现状**：案件表单与列表中字段标签为"板块"（数据库字段 `cases.section`）。

**目标规格**：

| 改动点 | 现状 | 目标 |
|---|---|---|
| 案件表单标签 | 板块 | **部门** |
| 案件列表表头 | 板块 | **部门** |
| 数据信息库/导出列名 | 板块 | **部门** |
| 数据库字段 | `section` | **保持不变**（仅前端文案更名，避免数据迁移风险） |
| 历史数据 | 旧值如"砂浆""工程部" | 原样展示，不强制清理 |

**注意**：字段更名仅涉及显示文案与导出表头；`section` 字段名、API 参数名（`section`）保持不变，避免破坏导入模板、Excel 导出、数据信息库等既有功能。

**验收标准**：
- [ ] 系统内（表单、列表、导出、查阅）已无"板块"字样，统一显示"部门"；
- [ ] 旧数据正常显示，导入模板中映射"板块"列名的模板仍可复用（或提示用户改列名）。

### 1.4 收费标准字段附加单位"人民币元"

**现状**：`收费标准`（`cases.fee_standard`）为自由文本输入框，无单位提示。

**目标规格**：

| 规格项 | 说明 |
|---|---|
| 输入框 | 在输入框右侧附加固定后缀"人民币元"（非占位符），用户输入时可见单位 |
| 展示处 | 案件列表、详情、结案审批表、薪酬试算等处，涉及收费标准展示的均带"人民币元"单位 |
| 存储 | `fee_standard` 字段仍存纯文本（如"本金5%+违约金冲抵"），**不拼接单位入库**，避免污染数据与检索 |
| 兼容 | 已有数据（含"元""￥"等旧表述）原样展示，不重复追加单位 |

**实现建议**：输入框使用 `input-group`（输入框 + 前置/后置标签），后端输出收费标准时不改值，仅前端渲染单位；导出 Excel 时表头写"收费标准（人民币元）"。

**验收标准**：
- [ ] 新建/编辑案件时，收费标准输入框右侧常驻"人民币元"后缀；
- [ ] 保存后数据库字段不含单位后缀（如仍为"本金5%+违约金冲抵"）；
- [ ] 列表与审批表展示处带有单位，不重复叠加。

---

## 2. 薪酬计算模块

### 2.1 页面双区域布局：待结算 / 已结算

**现状**：薪酬计算页（`#page-salary`）以案件卡片流渲染全部案件，顶部仅有一个结算月筛选框。

**目标规格**：

```
┌──────────────────────────────────────────────┐
│ 薪酬计算（结算月筛选：[2026-09 ▾] [显示全部]）        │
├──────────────────┬───────────────────────────┤
│ 【待结算案件】       │ 【已结算案件】（按月筛选）        │
│ ┌──────────────┐ │ ┌──────────────────────┐ │
│ │ 案号/客户/回款  │ │ 结算单批次/案号/人员/金额  │ │
│ │ 成员表格+金额   │ │ 按结算月筛选 + 导出       │ │
│ └──────────────┘ │ └──────────────────────┘ │
│   [一键结算全部]    │                           │
└──────────────────┴───────────────────────────┘
```

**区域划分判定规则**（核心逻辑）：

| 区域 | 判定条件 | 数据来源 |
|---|---|---|
| 待结算 | `settle_ready=1`（已确认回款金额 > 0 且有小组成员）**且** 该案件在 `settlements` 表中无结算记录 | `cases` + `case_members` |
| 已结算 | 该案件在 `settlements` 表中存在结算记录 | `settlements` + `cases` |

**补充规则**：
1. 同一案件同一结算月只进入一个区域；若案件结算后又被修改回款金额，以 `settlements` 记录为准仍归"已结算"，如需重算需先删除对应结算记录（管理端操作）；
2. 待结算区域顶部显示汇总条：待结算案件数、涉及回款总额、预计薪酬合计；
3. 已结算区域支持**按月份筛选**：下拉列出所有结算月份（按结算单创建月份去重），默认展示最近 6 个月，可选"全部"；
4. 已结算区域每行展示：批次号、案号、客户、成员、结算金额、生成时间、下载/重开结算单按钮；
5. 顶部结算月筛选框（`salaryMonth`）仍然保留，作用于**待结算区域**（按提成结算月筛选），与已结算区域月份筛选互不干扰。

**验收标准**：
- [ ] 薪酬页默认同时展示两个区域，无案件时显示空态提示；
- [ ] 有结算记录的案件从待结算区消失、出现在已结算区；
- [ ] 已结算区域按月筛选生效，默认最近 6 个月；
- [ ] 待结算案件的一键结算操作与已结算区域互不影响。

### 2.2 待结算案件完整信息展示

**现状**：待结算卡片已展示案号、客户、回款、回款月/结算月、成员表格（角色/姓名/提成率/保底/固定/计算过程/金额）、合计。

**目标规格**：待结算案件卡片必须**完整**展示薪酬计算公式所需全部信息，逐项如下：

| 展示项 | 来源 | 示例 |
|---|---|---|
| 案号 | `cases.case_no` | 001 |
| 客户名称 | `cases.client_name` | 某项目客户 |
| 项目回款金额 | `cases.confirmed_amount`（回退费用收回合计） | ¥100,000.00 |
| 回款月 / 结算月 | `recover_month` / `settle_month` | 2026-08 / 2026-09 |
| 小组成员（自动索引） | `case_members`（按案件小组名单自动匹配） | 张三 / 李四 / 王五 |
| 成员角色 | `case_members.role` | 维护人 / 主承办人 / 协办人 |
| 各成员提成比例 | `case_members.rate`（默认花名册引入，可改） | 8% / 4% / 1% |
| 保底金额 | `case_members.guaranteed_amount` | 0 / 600 / 600 |
| 固定金额 | `case_members.fixed_amount` / 薪酬规则 | 0 / 0 / 0 |
| 计算过程 | 后端生成公式串 | max(100000×8%, 0)+0=8000 → 取整 8000 |
| 最终薪酬 | `round_half_up(max(回款×率, 保底)+固定)` | 8000 / 4000 / 1000 |
| 案件薪酬合计 | 各成员金额之和 | ¥13,000.00 |
| 结算状态标签 | `settle_ready` | 可结算（绿色） |

**示例场景（001 号案件）**：

> 001 号案件，维护人张三（提成 8%）、主承办人李四（4%）、协办人王五（1%），项目回款 10 万元。系统自动将案件跳转至薪酬计算页，待结算区域完整展示：

| 案号 | 客户 | 回款(¥) | 结算月 | 角色 | 成员 | 提成率 | 保底 | 固定 | 计算过程 | 薪酬(¥) |
|---|---|---|---|---|---|---|---|---|---|---|
| 001 | 某项目客户 | 100,000.00 | 2026-09 | 维护人 | 张三 | 8% | 0 | 0 | max(100000×8%,0)+0=8000→取整 | **8,000** |
| 001 | 某项目客户 | 100,000.00 | 2026-09 | 主承办人 | 李四 | 4% | 600 | 0 | max(100000×4%,600)+0=4000→取整 | **4,000** |
| 001 | 某项目客户 | 100,000.00 | 2026-09 | 协办人 | 王五 | 1% | 600 | 0 | max(100000×1%,600)+0=1000→取整 | **1,000** |

本案薪酬合计：**¥13,000.00**。

**验收标准**：
- [ ] 待结算案件卡片展示上表全部字段，无缺项；
- [ ] 小组成员按案件小组名单自动索引，无需手动重复录入；
- [ ] 金额按 `round_half_up` 四舍五入取整（1720.5→1721，非银行家舍入）；
- [ ] 回款金额为 0 或未达条件案件不出现在待结算区。

---

## 3. 结算与文档生成模块

### 3.1 计算后自动转入结算页

**现状**：结算页（`#page-settle`）需用户手动勾选案件后生成，与薪酬计算页相互独立。

**目标规格**：

| 规格项 | 说明 |
|---|---|
| 自动转入 | 待结算案件在薪酬计算页完成计算后，**默认勾选**出现在结算页"批量生成结算单"列表中（现已有默认勾选可结算案件，继续维持） |
| 数据联动 | 结算金额 = 薪酬计算页该案件成员薪酬合计，两页数据同源（同一后端计算逻辑），保证一致 |
| 一键结算 | 结算页提供"勾选所有可结算案件" + "一键结算并生成文档"按钮，生成后案件自动移入已结算区 |
| 结算留痕 | 每次生成记录批次号（`JS+时间戳`）、案件、成员、金额、文档路径、操作人、时间 |

### 3.2 批量生成结算文档（模板机制）

**现状**：已支持默认模板（内置 docx 渲染）+ 自定义模板（`data/templates/` 目录，系统设置指定文件名）。

**目标规格**：

| 模板类型 | 机制 | 说明 |
|---|---|---|
| 默认模板 | 内置 `render_settlement_docx` | 系统自带版式，无需配置，开箱即用 |
| 自定义模板 | `data/templates/*.docx` | 在系统设置"结算单模板文件名"指定，按占位符渲染 |

**自定义模板占位符规范**（与现状保持一致并文档化）：

```
{{case_no}}      案号
{{client_name}}  客户名称
{{user_name}}    成员姓名
{{amount}}       结算金额（¥，取整）
{{date}}         结算日期
{{batch_no}}     批次号
{{firm}}         律所名称（系统设置）
```

**批量生成流程**：

```
用户在结算页勾选案件
      ↓
点击「批量生成结算单（Word）」
      ↓
后端按 案件 → 成员 逐份渲染（每成员 1 份 docx）
      ↓
生成 ZIP 打包，浏览器自动下载
      ↓
settlements 表记录生成明细，案件转入"已结算"区
```

**验收标准**：
- [ ] 薪酬计算完成后，结算页列表自动出现可结算案件且默认勾选；
- [ ] 一键结算成功生成每成员一份结算文档并打包 ZIP 下载；
- [ ] 自定义模板按占位符正确替换，金额与薪酬计算页一致；
- [ ] 生成后案件在薪酬页显示于"已结算"区域，可按月份筛选查看。

---

## 4. 数据模型与接口变更

### 4.1 数据模型

本次需求**无需新增数据库字段**（结算状态以 `settlements` 表记录为准；部门/单位均为展示层改动）。仅补充约定：

| 表 | 字段 | 说明 |
|---|---|---|
| `cases` | `section` | 保留原名，展示层更名"部门" |
| `cases` | `fee_standard` | 保留纯文本，展示层附加"人民币元" |
| `cases` | `settle_ready` | 待结算判定依据（confirmed_amount>0 且有成员） |
| `settlements` | `batch_no/case_no/user_name/amount/created_at` | 已结算区域数据来源 |

**发票 AI 识别规范对齐（新增字段，自动迁移）**：invoices 表新增 `total_amount / total_amount_cn / tax_rate / buyer_tax_id / seller_tax_id / drawer / remark` 七字段，与《发票AI识别提示词（整合版）》字段映射完全一致；历史数据的 `amount`（原价税合计口径）自动迁入 `total_amount`，开票金额比对（`GET /api/invoices/compare`）同步改用价税合计口径。

### 4.2 接口变更

| 接口 | 变更 | 说明 |
|---|---|---|
| `GET /api/salary/calc` | 扩展响应 | 增加 `settled`（是否已结算）、`settle_batch`（批次号，可空）字段，供双区域渲染 |
| `GET /api/settlements` | 扩展参数 | 支持 `month=YYYY-MM` 按结算月份筛选、`case_id` 过滤 |
| `GET /api/cases` | 不变 | 前端由 `settlements` 数据推断已结算 |
| `POST /api/settlements/generate` | 不变 | 维持一键生成，返回批次号与下载链接 |

### 4.3 前端存储

| 用途 | key | 说明 |
|---|---|---|
| 案件弹窗尺寸记忆 | `caseModalSize` | `{w,h}`，关闭时写入、打开时读取 |

---

## 5. UI / 交互规格汇总

| 页面 | 交互 | 规格 |
|---|---|---|
| 案件弹窗 | 拖拽调宽 | 右下角 resize 手柄，min 720px / max 90vw，localStorage 记忆 |
| 案件弹窗 | 长文本 | 全局 `word-break`，禁止横向滚动条（表格除外） |
| 案件弹窗 | 案件类型 | 民事分类下拉 + 手动补充输入 |
| 案件弹窗 | 部门/收费标准 | 标签"部门"；收费标准输入框带"人民币元"后缀 |
| 薪酬页 | 双区域 | 待结算（左/上）+ 已结算（右/下），各自独立筛选 |
| 薪酬页 | 汇总条 | 待结算件数、回款总额、预计薪酬合计 |
| 结算页 | 一键结算 | 默认勾选可结算案件，生成 ZIP 下载 |

---

## 6. 实施计划

| 阶段 | 任务 | 工作量（估） | 产出 |
|---|---|---|---|
| 阶段 1：案件管理 | 1.1 弹窗拖拽调宽+换行；1.2 案件类型下拉；1.3 部门更名；1.4 收费标准单位 | 0.5~1 天 | 前端改造 + 回归测试 |
| 阶段 2：薪酬双区域 | 2.1 后端 `settled` 字段；2.2 前端双区域布局与筛选 | 0.5~1 天 | 薪酬页重构 |
| 阶段 3：结算联动 | 3.1 自动转入确认；3.2 模板批量生成完善 | 0.5 天 | 结算闭环回归 |
| 阶段 4：验收 | 示例场景端到端测试 + 数据清理 | 0.5 天 | 验收报告 |

**关键实施要点**：
1. 案件类型下拉使用 `datalist` 实现"下拉+可输入"双能力，避免限定死案由；
2. 部门更名仅改文案层，不迁移数据、不改 API 字段名；
3. 待/已结算判定统一在后端 `salary_calc` 完成（避免前端多次请求不一致）；
4. 结算金额必须与薪酬页同源计算（复用同一公式函数），杜绝两处不一致；
5. 全部改动通过 `migrate_db()` 保持旧库兼容，测试数据清理后交付初始状态。

---

## 7. 风险与注意事项

| 风险 | 影响 | 对策 |
|---|---|---|
| 旧数据案件类型不规范 | 下拉无法完整映射 | 下拉可手动输入；旧值原样展示 |
| 已结算案件重算 | 金额变更产生不一致 | 已结算案件禁止直接编辑回款金额，需先删除结算记录 |
| 拖拽布局破坏现有表单 | 保存/成员管理异常 | 拖拽仅作用于容器尺寸，不改 DOM 结构，回归测试覆盖 |
| 自定义模板占位符缺失 | 结算单内容不全 | 模板校验：未识别占位符给出警告 |

---

## 8. 数据自动备份

> 完整设计与配置说明见《数据自动备份功能设计方案.md》，操作见《操作人员操作指引.md》2.7 节。

### 8.1 功能概述

支持自定义备份路径、可配置执行周期、可选备份范围、进度反馈与日志、过期备份自动清理、失败可读错误提示。

### 8.2 配置项

| 配置项 | 默认值 | 说明 |
|---|---|---|
| `backup_enabled` | `0` | 自动备份总开关（关闭时仍可手动立即备份） |
| `backup_path` | 空（默认 `数据目录/backups`） | 备份目录，建议配置到其他磁盘 |
| `backup_schedule` | `daily` | 周期：`daily` 每日 / `weekly` 每周 / `interval` 按间隔小时 / `manual` 仅手动 |
| `backup_time` | `23:00` | daily/weekly 的每日触发时刻 |
| `backup_weekday` | `5` | weekly 模式执行星期（0=周一 … 6=周日） |
| `backup_interval_hours` | `24` | interval 模式间隔小时数 |
| `backup_keep` | `10` | 保留份数，`0` = 不自动清理 |
| `backup_scope` | 全选 | 范围：`db` 数据库 / `uploads` 上传文件 / `templates` 结算模板 / `config` AI 配置 |

### 8.3 备份文件格式

`coop_backup_YYYYMMDD_HHMMSS.zip`，含 `coop.db`（SQLite 在线一致性快照）、`uploads/`、`templates/`、`config/ai_providers.json`、`manifest.json`（创建时间/触发方式/范围/条目清单）。

### 8.4 执行与保障机制

- 后台守护线程每 60s 检查调度，按 `_next_run_seconds` 计算下次触发；`interval` 模式按上次运行时间。
- 并发保护：全局非阻塞锁，同一时刻仅一个备份任务；临时文件 + 原子改名。
- 进度反馈：前端 1.5s 轮询 `/api/backup/status`，展示阶段文案与进度条。
- 日志：`backup.log`（备份目录下，失效时回退默认目录），界面展示最近 200 行。
- 过期清理：每次备份成功后按保留份数自动删除最旧文件；亦支持手动触发。
- 失败提示：Windows 错误码映射中文（权限/磁盘满/路径无效/文件占用），含失败阶段定位。

### 8.5 接口一览

| 方法 | 路径 | 功能 |
|---|---|---|
| GET/PUT | `/api/backup/config` | 读取/保存配置 |
| POST | `/api/backup/run` | 立即备份（异步） |
| GET | `/api/backup/status` | 状态 + 上次结果（轮询） |
| GET | `/api/backup/list` | 备份文件列表 |
| GET | `/api/backup/download/<name>` | 下载备份（文件名正则白名单） |
| DELETE | `/api/backup/delete/<name>` | 删除备份 |
| POST | `/api/backup/cleanup` | 手动清理过期备份 |
| GET | `/api/backup/log` | 最近 200 行日志 |

所有接口要求「设置管理」权限；下载/删除接口做文件名白名单校验防路径穿越。
