# 律师事务所业务管理系统 — 案件与薪酬模块优化功能规格说明（v1.5）

| 项目 | 内容 |
|---|---|
| 文档版本 | v1.5 |
| 编制日期 | 2026-08-31 |
| 适用系统 | 律所业务协同管理系统（coop-base） |
| 适用对象 | 产品、开发、测试、业务验收 |
| 变更范围 | 案件管理模块、薪酬计算模块、结算与文档生成模块、**案件看板模块（v1.5 新增）** |
| 兼容性 | 所有改动通过 `migrate_db()` 自动迁移，旧数据不丢失 |

---

## 一、背景与目标

本所主营**民事案件**，现有案件与薪酬模块在录入体验、字段口径、结算流程闭环上仍有不足，需优化以贴近实际业务操作习惯，保证数据一致性与用户体验流畅。

本规格涵盖四大模块：
1. **案件管理模块** — 录入窗口、字段口径、单位规范
2. **薪酬计算模块** — 待结算/已结算分区展示、公式完整呈现
3. **结算与文档生成** — 自动转入、批量生成结算文档
4. **案件看板模块**（v1.5 新增） — 自定义时间段的案件/回款/合同统计、历史年度对比、项目进度与人员业绩

---

## 二、案件管理模块

### 2.1 新建/编辑案件窗口支持拖拽调整宽度

**需求描述**：当前案件弹窗固定宽度，字段内容较长时被截断，影响信息完整展示。

**功能规格**：

| 编号 | 规格项 | 验收标准 |
|---|---|---|
| 2.1.1 | 弹窗宽度可拖拽 | 弹窗右侧边缘可鼠标拖拽调整宽度，最小宽度 600px，最大宽度为视口宽度 95% |
| 2.1.2 | 宽度持久化 | 调整后的宽度保存至 `localStorage`，下次打开弹窗时自动恢复 |
| 2.1.3 | 字段自动换行 | 表单 `input/textarea` 超长内容自动换行，不出现省略号或截断 |
| 2.1.4 | 表单响应式 | 弹窗宽度变化时，表单字段网格自适应列数（窄屏单列、宽屏多列） |
| 2.1.5 | 列表字段换行 | 案件列表单元格内容超长时自动换行展示，行高自适应 |

**技术实现要点**：
- 弹窗外层容器加 `resize: horizontal; overflow: auto;` CSS 属性
- 监听 `mouseup` 事件将宽度写入 `localStorage.setItem('caseModalWidth', w)`
- 打开弹窗时 `openCaseModal()` 读取并应用存储的宽度
- 表单容器 `form-row` 使用 `grid-template-columns: repeat(auto-fit, minmax(220px, 1fr))` 自适应

### 2.2 案件类型按民事案件分类设置

**需求描述**：原"案件类型"为自由文本输入，无统一标准。现按本所主营民事案件分类设置下拉选项。

**功能规格**：

| 编号 | 规格项 | 验收标准 |
|---|---|---|
| 2.2.1 | 字段类型变更 | "案件类型"由文本输入改为**下拉选择** |
| 2.2.2 | 民事案件分类选项 | 默认选项如下，支持在系统设置中扩展： |

**民事案件分类选项（默认）**：

```
婚姻家庭纠纷
继承纠纷
合同纠纷
侵权责任纠纷
劳动争议
人事争议
物权纠纷
担保物权纠纷
知识产权纠纷
不正当竞争纠纷
公司有关纠纷
合伙企业纠纷
破产纠纷
建设工程合同纠纷
房屋买卖合同纠纷
房屋租赁合同纠纷
物业服务合同纠纷
民间借贷纠纷
保证合同纠纷
其他民事纠纷
```

| 编号 | 规格项 | 验收标准 |
|---|---|---|
| 2.2.3 | 选项可配置 | 系统设置页新增"案件类型选项"管理，支持增删改，存储于 `settings.case_types`（JSON 数组） |
| 2.2.4 | 旧数据兼容 | 历史案件 `case_type` 不在选项中的，编辑时显示原值并允许保留 |

### 2.3 "板块"字段更名为"部门"

**需求描述**：原"板块"字段语义不清晰，更名为"部门"以贴合律所组织架构。

**功能规格**：

| 编号 | 规格项 | 验收标准 |
|---|---|---|
| 2.3.1 | 字段更名 | 案件弹窗、案件列表、数据信息库中所有"板块"标签更名为"**部门**" |
| 2.3.2 | 部门下拉 | "部门"改为下拉选择，选项从花名册 `staff.department` 去重自动生成 |
| 2.3.3 | 数据库字段 | 数据库字段名 `section` 保持不变（仅展示层更名），无需迁移 |
| 2.3.4 | 全局一致 | 系统所有页面（表单/列表/详情/导出/数据信息库）统一显示"部门" |

### 2.4 收费标准字段自动附加单位"人民币元"

**需求描述**：收费标准字段缺乏单位标识，易造成歧义。

**功能规格**：

| 编号 | 规格项 | 验收标准 |
|---|---|---|
| 2.4.1 | 单位后缀 | "收费标准"输入框右侧固定显示后缀"**人民币元**"（readonly 文字，不可编辑） |
| 2.4.2 | 输入约束 | 仅允许输入数字、小数点、百分号及相关说明字符（如"本金5%+违约金冲抵"） |
| 2.4.3 | 展示一致 | 案件列表、详情、数据信息库、Word 导出中"收费标准"均带"人民币元"后缀 |
| 2.4.4 | 后缀样式 | 后缀文字以浅色 `hint` 样式展示，与输入内容区分 |

**实现方式**：
```html
<div class="form-item">
  <label>收费标准</label>
  <div style="display:flex;align-items:center">
    <input id="cFeeStandard" style="flex:1">
    <span class="hint" style="white-space:nowrap;margin-left:6px">人民币元</span>
  </div>
</div>
```

---

## 三、薪酬计算模块

### 3.1 页面展示：单区域卡片按结算月归集（v1.4 已对齐实际实现）

**需求描述**：薪酬计算页以案件为单元展示薪酬试算结果，按"提成结算月"筛选，以卡片左边界色区分可结算状态。

**功能规格**：

| 编号 | 规格项 | 验收标准 |
|---|---|---|
| 3.1.1 | 展示结构 | 薪酬试算结果为**单区域卡片列表**：每个案件一张卡片（无待结算/已结算双分区） |
| 3.1.2 | 状态区分 | 卡片左边界色标识：`settle_ready=true`（财务已确认回款金额且有成员）为**绿色**，否则为蓝色 |
| 3.1.3 | 可结算标签 | 可结算案件卡片头部显示绿色"可结算"标签 |
| 3.1.4 | 筛选维度 | 顶部筛选控件为**提成结算月**（month 选择器），另提供"重新计算"与"显示全部"按钮 |
| 3.1.5 | 数据归集 | `/api/salary/calc` 返回平坦案件列表；留空筛选月份时显示全部案件 |
| 3.1.6 | 已结算查询 | 已生成的结算单在"薪酬结算"页按批次号/月份查阅（非本页分区展示） |

**页面布局示意（与实际一致）**：

```
┌─────────────────────────────────────────────────────┐
│ 薪酬试算结果（按结算月归集）                          │
│ [提成结算月: 2026-09 ▼] [重新计算] [显示全部]        │
├─────────────────────────────────────────────────────┤
│ ▎案号 001 某项目客户  项目回款：¥100,000              │
│ ▎回款月 2026-09 · 结算月 2026-09     [可结算]        │  ← 左边界绿色
│ │ 角色 │项目组成员│提成比例│保底金额│固定金额│计算过程│薪酬金额│操作 │
│ │ 维护人│张三    │8.00%  │0.00   │0.00   │max(...)│8,000 │调整 │
│ │ 主承办│李四    │4.00%  │600.00 │500.00 │max(...)│4,500 │调整 │
│ │ 协办人│王五    │1.00%  │1,000.00│0.00  │max(...)│1,000 │调整 │
│ 本案薪酬合计：¥ 13,500                              │
└─────────────────────────────────────────────────────┘
```

### 3.2 案件卡片完整展示薪酬计算公式全部信息（v1.4 已对齐实际实现）

**需求描述**：每个案件卡片展示公式所需全部输入项与计算结果，确保财务可核对。

**功能规格**：

| 编号 | 规格项 | 验收标准 |
|---|---|---|
| 3.2.1 | 案件头信息 | 展示：案号、客户名称、项目回款金额（¥）、财务回款月、提成结算月、是否可结算标签 |
| 3.2.2 | 成员表格列 | 完整展示 **8 列**：角色、项目组成员、提成比例、保底金额(¥)、固定金额(¥)、计算过程、薪酬金额(¥)、操作 |
| 3.2.3 | 计算过程明文 | 计算过程列展示完整算式，例：`max(100000.00 × 8.00%, 保底0.00) + 固定0.00 = 8000.00 → 取整 8000` |
| 3.2.4 | 提成率显示 | 提成比例列显示百分比（如 `8.00%`），2 位小数 |
| 3.2.5 | 保底/固定 | 保底金额、固定金额均显示具体数值（¥），不显示空白 |
| 3.2.6 | 金额取整 | 最终薪酬金额为四舍五入取整后的整数（`round_half_up`） |
| 3.2.7 | 手动调整标记 | 如该成员金额被手动调整，金额后显示 `已调整` 红色标签 |
| 3.2.8 | 案件合计 | 每个案件卡片底部显示"本案薪酬合计：¥ X" |
| 3.2.9 | 手动调整 | 操作列"调整"按钮打开调整弹窗（v1.2 起为模态框表单），留空可恢复自动计算 |

### 3.3 薪酬公式定义（统一标准）

**公式**：
```
薪酬金额 = max(到账金额 × 提成率, 保底金额) + 固定金额
结果四舍五入取整（ROUND_HALF_UP，非银行家舍入）
```

**字段说明**：

| 字段 | 来源 | 默认值 | 可修改 |
|---|---|---|---|
| 到账金额 | `cases.confirmed_amount`（财务确认回款金额） | 0 | 财务在案件财务模块修改 |
| 提成率 | `case_members.rate` | 从花名册 `staff.commission_rate` 按姓名自动引入 | 案件成员模块可改 |
| 保底金额 | `case_members.guaranteed_amount` | 维护人 0 元、其他成员 600 元 | 案件成员模块可改 |
| 固定金额 | `case_members.fixed_amount` 或 `salary_rules.fixed_amount` | 0 | 成员模块或薪酬规则配置 |

**示例场景（验收用例）**：

案件 001，项目回款 ¥100,000，小组成员：
- 张三（维护人，提成 8%，保底 0 元，固定 0 元）
  → `max(100000 × 8%, 0) + 0 = 8000` → 取整 **8000**
- 李四（主承办人，提成 4%，保底 600 元，固定 500 元）
  → `max(100000 × 4%, 600) + 500 = 4500` → 取整 **4500**
- 王五（协办人，提成 1%，保底 1000 元，固定 0 元）
  → `max(100000 × 1%, 1000) + 0 = 1000` → 取整 **1000**

本案薪酬合计：¥ 13,500

---

## 四、结算与文档生成

### 4.1 待结算案件自动转入结算页面

**需求描述**：达到结算条件的案件自动出现在结算页面，财务可批量勾选结算。

**功能规格**：

| 编号 | 规格项 | 验收标准 |
|---|---|---|
| 4.1.1 | 自动转入 | 案件 `settle_ready=true` 后自动出现在结算页"可结算案件"列表 |
| 4.1.2 | 默认勾选 | 可结算案件复选框默认勾选，便于一键结算 |
| 4.1.3 | 颜色标识 | 可结算行绿色高亮（`.settle-ready` 样式），不可结算灰色 |
| 4.1.4 | 一键全选 | 表头复选框支持"勾选所有可结算案件" |
| 4.1.5 | 一键结算 | 点击"批量生成结算单"后，按勾选案件逐成员生成结算单 |
| 4.1.6 | 数据归集 | 结算时自动按 case_members 成员 + 薪酬公式计算金额，四舍五入取整 |
| 4.1.7 | 批次号 | 每次结算生成唯一批次号（格式 `JS + YYYYMMDDHHmmss`） |
| 4.1.8 | 防重复 | 同一案件同一批次不重复生成；已结算案件在待结算列表中消失 |

### 4.2 批量生成结算文档

**需求描述**：支持按自定义模板或默认模板批量生成结算文档（Word）。

**功能规格**：

| 编号 | 规格项 | 验收标准 |
|---|---|---|
| 4.2.1 | 模板选择 | 结算页提供模板下拉：默认模板 + 已配置的自定义模板 |
| 4.2.2 | 默认模板 | 系统内置默认结算单模板（含案号/组员/金额/日期/律所名称） |
| 4.2.3 | 自定义模板 | 模板管理页可新建模板，配置显示字段、字段顺序、标题与落款 |
| 4.2.4 | 生成格式 | 每位成员一份 `.docx` 结算单，按 `批次号_案号_姓名.docx` 命名 |
| 4.2.5 | 打包下载 | 多份结算单打包为 ZIP 下载，文件名 `结算单_批次号.zip` |
| 4.2.6 | 结算记录 | 生成后写入 `settlements` 表（batch_no/case_id/case_no/user_name/amount/doc_path） |
| 4.2.7 | 关联跳转 | 结算记录列表可点击下载历史结算单文件 |

### 4.3 完整流程闭环

```
案件管理                  薪酬计算                    结算生成
┌──────────┐         ┌──────────────┐          ┌──────────────┐
│ 录入案件   │         │ 待结算区域    │          │ 可结算列表    │
│ +成员+回款 │──判断──>│ 展示完整公式  │──勾选──> │ 默认勾选      │
│ +结算月    │  可结算  │ +计算结果    │  结算    │ 一键生成      │
└──────────┘         └──────────────┘          └──────┬───────┘
                           │                          │
                      手动调整金额                    生成
                           │                          │
                           ▼                          ▼
                     已结算区域                  Word/ZIP 下载
                     按月份筛选                  settlements 记录
```

---

## 五、数据模型与接口

### 5.1 数据模型变更

| 表 | 变更 | 说明 |
|---|---|---|
| `cases` | `section` 字段语义改为"部门"（仅展示层更名） | 无需数据库迁移 |
| `settings` | 新增 `case_types`（JSON 数组） | 民事案件类型选项 |
| 其他 | 无新增表/字段 | 复用现有 `case_members`/`salary_adjustments`/`settlements` |

### 5.2 接口变更

| 接口 | 方法 | 变更说明 |
|---|---|---|
| `/api/settings` | GET/PUT | 新增 `case_types` 字段读写（v1.3） |
| `/api/case-types` | GET | 新增：案件类型下拉选项只读接口（v1.3，登录即可读） |
| `/api/staff` | GET | 部门选项从 `staff.department` 去重返回 |
| `/api/salary/calc` | GET | 返回平坦案件列表（`data` 数组），支持 `?month=YYYY-MM` 按提成结算月筛选 |
| `/api/cases` | GET | `kw` 模糊匹配七字段：客户/案号/负责人/开发人/案由/原告/被告（v1.3） |
| `/api/settlements/generate` | POST | 不变（已有） |
| `/api/dashboard` | GET | 新增：案件看板数据接口，参数 `start` / `end` / `dim`（v1.5） |

### 5.2.1 案件看板接口（v1.5 新增）

**请求**：`GET /api/dashboard?start=YYYY-MM-DD&end=YYYY-MM-DD&dim=owner|developer|member`

| 参数 | 必填 | 默认 | 说明 |
|---|---|---|---|
| `start` | 否 | 当月 1 日 | 统计起始日（含） |
| `end` | 否 | 今日 | 统计截止日（含）；若 `end < start` 则自动交换 |
| `dim` | 否 | `owner` | 人员口径：`owner` 主承办人 / `developer` 开发人 / `member` 小组成员（取 `case_members.person`） |

**统计口径**（区间 = `[start, end]`）：

| 指标 | 计算规则 |
|---|---|
| 立项 | `created_at ∈ 区间` |
| 结项 | `status='已结案'` 且 `updated_at ∈ 区间` |
| 暂停 | `status='暂停'` 且 `updated_at ∈ 区间` |
| 进行中 | `status='进行中'` 且 `created_at ≤ end`（期间案件中截至期末仍在办） |
| 期间案件 | 区间内有建档、状态更新或回款记录的案件 |
| 期末在册 | `created_at ≤ end` 且（`status≠'已结案'` 或 `updated_at > end`） |
| 回款金额 | `fee_recoveries.recovery_date ∈ 区间` 的 `amount` 合计 |
| 代理费（合同额） | 期间案件的 `agent_fee` 合计 |
| **回款率** | **区间回款金额 ÷ 区间案件代理费 × 100%**（分母为 0 时返回 `null`，前端显示 `—`） |

> **口径设计说明**：接口区分「期间口径」与「期末口径」。`new` / `closed` / `paused` / `doing` / `fee` / `recovery` / `rate` 为区间事件指标；`doing_all` / `paused_all` / `closed_all` / `listed` 为期末快照指标，**不受期间案件筛选影响**。这样上年度立项、本月无动作但仍在办的案件，仍会计入本月「在办」数量。

**返回结构**：

```json
{
  "ok": true,
  "data": {
    "range":   { "start": "2026-08-01", "end": "2026-08-31", "dim": "owner", "dim_label": "主承办人" },
    "summary": { "total": 3, "new": 1, "closed": 1, "paused": 1, "doing": 1,
                 "listed": 3, "doing_all": 2, "paused_all": 1, "closed_all": 2,
                 "fee": 230000.0, "recovery": 130000.0, "invoice": 140000.0,
                 "subject": 1000000.0, "rate": 56.5,
                 "new_fee": 100000.0, "cum_fee": 410000.0,
                 "cum_recovery": 290000.0, "cum_rate": 70.7 },
    "yearly":  [{ "year": "2026", "new": 3, "closed": 1, "fee": 230000.0,
                  "recovery": 170000.0, "rate": 73.9,
                  "yoy_new": null, "yoy_fee": null, "yoy_recovery": null }],
    "monthly": [{ "month": "2026-08", "new": 1, "closed": 1,
                  "fee": 100000.0, "recovery": 130000.0, "rate": 130.0 }],
    "projects":[{ "name": "民商诉讼", "total": 2, "new": 1, "closed": 1, "paused": 0,
                  "doing": 1, "fee": 150000.0, "recovery": 110000.0,
                  "rate": 73.3, "close_rate": 50.0 }],
    "persons": [ /* 字段同 projects，分组键由 dim 决定 */ ]
  }
}
```

字段补充：`yearly` 为**全量历史**年度（不受区间筛选），按年份倒序，次年行带 `yoy_*` 同比（相对上一行年度，上期为 0 或首年时为 `null`）；`monthly` 仅取区间内月份；`projects` / `persons` 按 `name` 字典序作次要排序，主排序为代理费降序；`close_rate` = 期末已结案数 ÷ 期间案件数 × 100%。

### 5.3 薪酬计算接口返回结构（平坦列表，与实际一致）

```json
{
  "ok": true,
  "data": [
    {
      "case_id": 2, "case_no": "001", "client_name": "某项目客户",
      "recovered": 100000.00, "recover_month": "2026-09",
      "settle_month": "2026-09", "settle_ready": true,
      "member_count": 3,
      "items": [
        {"member_id": 1, "person": "张三", "role": "maintainer", "role_name": "维护人",
         "rate": 0.08, "guaranteed_amount": 0, "fixed": 0, "base": 8000.00,
         "amount": 8000, "adjusted": false,
         "calc": "max(100000.00 × 8.00%, 保底0.00) + 固定0.00 = 8000.00 → 取整 8000"}
      ],
      "total": 13500
    }
  ]
}
```

> 注：已结算单据不在本接口返回，通过"薪酬结算"页按批次号/月份查阅 `settlements` 表。

---

## 六、非功能需求

| 编号 | 规格项 | 验收标准 |
|---|---|---|
| 6.1 | 数据一致性 | 人员信息唯一来源为 `case_members`，`cases.developer/owner/members` 为冗余同步只读字段 |
| 6.2 | 金额精度 | 所有薪酬金额经 `round_half_up`（ROUND_HALF_UP）取整，非 Python 默认银行家舍入 |
| 6.3 | 兼容性 | 字段更名/新增通过 `migrate_db()` 自动迁移，旧数据不丢失 |
| 6.4 | 权限 | 案件编辑需 `case.edit` 权限；财务字段需 `case.finance` 权限；薪酬查看需 `salary.view` |
| 6.5 | 审计 | 所有字段修改、结算生成操作写入审计日志 |
| 6.6 | UI 一致性 | 字段名称在表单/列表/详情/导出/数据信息库中全局一致 |

---

## 七、测试用例与验收（v1.5 已对齐实际实现）
### 7.0 现状差异对照总表
| 编号 | 差异位置 | 文档预期 | 实际现状 | 级别 | 修正建议 |
|---|---|---|---|---|---|
| D1 | 薪酬页布局 | 待结算/已结算双分区展示 | 单区域卡片，无分区 | 中 | ✅ v1.4 文档描述已对齐（第 3.1 节重写为单区域卡片） |
| D2 | 薪酬筛选 | 按自然月+结算状态筛选 | 筛选维度为"提成结算月" | 中 | ✅ v1.4 文档描述已对齐（第 3.1.4 条） |
| D3 | 薪酬成员明细 | 7 列 | 实际 8 列（含"操作"列） | 低 | ✅ v1.4 文档描述已对齐（第 3.2.2 条改为 8 列） |
| D4 | 调整薪酬交互 | 模态框表单 | ~~原生 prompt 输入~~ **v1.2 已改为模态框表单** | 中 | ✅ 已实施 |
| D5 | 案件弹窗字段名 | "部门" | ~~仍为"板块"~~ **v1.2 已全局更名"部门"** | 低 | ✅ 已实施 |
| D6 | 案件状态标签 | 三状态三套样式 | ~~"暂停"落入"进行中"样式~~ **v1.2 已新增暂停专属灰色标签** | 中 | ✅ 已实施 |
| D7 | 案件搜索范围 | 含案由/原告/被告 | ~~kw 仅匹配四字段~~ **v1.3 已扩展至七字段（客户/案号/负责人/开发人/案由/原告/被告）** | 低 | ✅ 已实施 |
| D8 | 案件类型/金额单位 | 类型下拉+单位后缀 | ~~未实施~~ **v1.3 已实施：类型下拉（settings.case_types 可配置）+ 收费标准"人民币元"后缀** | 低 | ✅ 已实施 |
### 7.1 案件管理模块测试用例（A1–A7）
| 编号 | 场景 | 步骤与输入 | 预期结果 | 实际核对 |
|---|---|---|---|---|
| A1 | 案件创建-自动案号 | 新建案件，客户名称"测试公司A"，案号留空，点击保存 | 保存成功，自动生成不重复案号，列表出现新记录 | ✅ 一致，next_case_no 自动递增 |
| A2 | 案件创建-手动案号查重 | 新建案件，录入已存在案号，点击保存 | 拒绝保存，提示"案号已存在"（HTTP 400） | ✅ 一致 |
| A3 | 案件创建-必填校验 | 客户名称留空，点击保存 | 拒绝保存，提示客户名称必填 | ✅ 一致 |
| A4 | 状态流转 | 编辑案件，状态从"进行中"改为"已结案"/"暂停"，保存后查看列表 | 状态正确保存并回显 | ✅ v1.2 起"暂停"显示专属灰色标签（D6 已修复） |
| A5 | 查询与筛选 | 搜索框分别输入客户名称/案号/负责人/开发人/案由/原告/被告关键词 | 命中字段模糊匹配的记录 | ✅ v1.3 起七字段均可搜（D7 已修复） |
| A6 | 详情展示 | 点击案件行打开详情/编辑弹窗 | 三模块完整回显：项目信息、项目组成员、财务信息 | ✅ 一致；字段名 v1.2 起为"部门"（D5 已修复） |
| A7 | 成员维护 | 在项目组中添加成员（上限10人）、修改角色、移除成员 | 增删改即时生效，不超上限 | ✅ 一致 |
### 7.2 规划功能用例（B1–B3，✅ v1.3 已全部实施）
| 编号 | 场景 | 预期结果 | 状态 |
|---|---|---|---|
| B1 | 成员拖拽排序 | 拖拽成员行调整顺序，顺序持久化 | ✅ v1.3 已实施（保存案件时经 /members/sync 全量重建持久化，GET 按 id 顺序返回） |
| B2 | 案件类型下拉 | 类型字段提供预设下拉选项 | ✅ v1.3 已实施（默认 20 类民事案由，系统设置可增删改；旧数据不在列表中时自动补入选项） |
| B3 | 金额单位后缀 | 收费标准输入框右侧显示"人民币元" | ✅ v1.3 已实施（浅色后缀，仅展示不入库） |
### 7.3 薪酬计算模块测试用例（S1–S7、C8）
| 编号 | 场景 | 步骤与输入 | 预期结果 | 实际核对 |
|---|---|---|---|---|
| S1 | 规则配置 | 在薪酬规则中设置成员提成比例/保底/固定额并保存 | 规则按成员正确保存与回显 | ✅ 一致 |
| S2 | 计薪周期 | 选择提成结算月，查看当月数据 | 仅统计该结算月已回款案件 | ✅ 一致（筛选维度即"提成结算月"，D2 已对齐） |
| S3 | 核心计算 | 001案回款 ¥100,000：张三8%、李四4%+固定500、王五1%保底1000 | 公式 max(回款×比例, 保底)+固定额，四舍五入取整；张三 8000、李四 4500、王五 1000，合计 ¥13,500 | ✅ 一致 |
| S4 | 保底触发 | 成员计算结果低于保底额 | 取保底额发放 | ✅ 一致（S3 中王五即触发样例） |
| S5 | 手动调整 | 点击调整薪酬，输入调整金额 | 调整额计入当期薪酬并留痕 | ✅ v1.2 起为模态框表单（D4 已修复） |
| S6 | 明细展示 | 展开成员薪酬明细 | 8 列明细与计算过程一致 | ✅ 一致（D3 已对齐为 8 列描述） |
| S7 | 可结算判定 | 查看案件结算状态 | 仅满足条件的案件进入可结算列表 | ✅ 一致（D1 已对齐：单区域卡片 + 绿色"可结算"标签） |
| C8 | 结算文档 | 执行结算并生成结算单 | 结算单金额与明细合计一致，结算后状态锁定 | ✅ 一致 |

### 7.4 案件看板模块测试用例（K1–K8，✅ v1.5 已全部通过）

> 验证数据：5 条跨年度案件（3 条 2026 年、2 条 2025 年，覆盖进行中/已结案/暂停三状态、3 个项目、3 名人员、6 笔回款）。

| 编号 | 场景 | 步骤与输入 | 预期结果 | 实际核对 |
|---|---|---|---|---|
| K1 | 默认区间 | 直接调用 `/api/dashboard` 不带参数 | 取当月 1 日 ~ 今日 | ✅ 一致（返回 `2026-08-01 ~ 2026-08-31`） |
| K2 | 期间流转统计 | `start=2026-08-01&end=2026-08-31` | 期间案件 3、立项 1、结项 1、暂停 1、期间进行中 1 | ✅ 一致 |
| K3 | 期末口径 | 同上区间 | 期末在册 3、在办 2、暂停 1、已结案 2（含 2025 年结案案件） | ✅ 一致，验证了期末口径不被期间筛选截断 |
| K4 | 回款率 | 同上区间 | 回款 130,000 ÷ 代理费 230,000 = 56.5% | ✅ 一致 |
| K5 | 累计口径 | 同上区间 | 累计合同额 410,000、累计回款 290,000、累计回款率 70.7% | ✅ 一致 |
| K6 | 历史年度同比 | 查看 `yearly` | 2026 年（新增 3、合同额 230,000、回款 170,000，同比为 `null`）；2025 年同比 案件 -33.3%、合同额 -21.7%、回款 -29.4% | ✅ 一致 |
| K7 | 项目维度分组 | 同上区间，查看 `projects` | 民商诉讼（2 件、回款率 73.3%、结案进度 50%）、刑事辩护（1 件、暂停 1、回款率 25%） | ✅ 一致 |
| K8 | 人员口径切换 | 分别以 `dim=owner` / `developer` / `member` 请求 | `owner` 返回 张三/李四；`developer` 返回 李四/王五/张三；`member` 返回 李四/张三/王五（取 `case_members.person`） | ✅ 一致 |

**前端渲染验证（Chrome 实测）**：侧边栏出现「案件看板」且位于「工作台」之后；点击后 `page-dashboard` 激活且全页仅 1 个 `.page.active`；9 张指标卡渲染；五个板块表格（含合计行）正常输出；切换「本年」快捷区间后年度表 2 行、项目表 3 行、人员表 2 行；切换人员口径至「小组成员」后表格正常重渲染；全程无 JS 报错。

**回归验证**：全页 `div` 开闭平衡 261=261；12 个 `page-*` 容器互为兄弟节点无嵌套（防止 v1.4 修复过的 `page-salary` 未闭合问题复发）；`py_compile app.py` 与内嵌 JS `node --check` 均通过。
---
## 八、实施排期
| 阶段 | 内容 | 优先级 | 关联差异/用例 |
|---|---|---|---|
| P1 | 文档描述对齐实际（D1/D2/D3/D5/D7） | 高 | 7.0 总表（✅ v1.4 已全部完成） |
| P2 | 暂停状态专属标签样式 | 中 | D6 / A4（✅ v1.2 已实施） |
| P2 | 调整薪酬改为模态框表单 | 中 | D4 / S5（✅ v1.2 已实施） |
| P2 | 弹窗字段"板块"更名"部门" | 中 | D5 / A6（✅ v1.2 已实施） |
| P3 | 案件类型下拉与金额单位后缀 | 低 | D8 / B2、B3（✅ v1.3 已实施） |
| P3 | 成员拖拽排序 | 低 | B1（✅ v1.3 已实施） |
| P3 | 案件搜索范围扩展（案由/原告/被告，可选） | 低 | D7 / A5（✅ v1.3 已实施） |
| P4 | 案件看板独立子页面（`/api/dashboard` + `page-dashboard`） | 中 | 第九节 / K1–K8（✅ v1.5 已实施） |
---
## 九、案件看板模块（v1.5 新增）

### 9.1 定位与入口

原工作台内嵌的「案件面板」信息密度低、不支持时间筛选，无法满足经营分析需要。v1.5 将其**升级为独立子页面**，并移除工作台中的旧卡片，避免同一功能两处维护。

| 项 | 内容 |
|---|---|
| 菜单位置 | 侧边栏第 2 项「案件看板」（紧随「工作台」之后） |
| 页面容器 | `#page-dashboard` |
| 所需权限 | `case.view` |
| 入口函数 | `loadDashboard()`，经 `showPage` 映射分发 |

### 9.2 页面结构

页面自上而下分为五个板块，全部数据由单一接口 `/api/dashboard` 一次返回：

| 序号 | 板块 | 内容 |
|---|---|---|
| 顶部 | 指标卡 | 9 张：期间案件 / 立项 / 结项 / 暂停 / 期间进行中 / 期末在办 / 期间回款 / 期间代理费 / 回款率 |
| 一 | 区间概览 | 回款率环形图 + 期末状态（在册/在办/暂停/已结案）+ 案件流转（立项/结项/暂停/进行中）+ 合同与回款（新签合同额、代理费、回款、开票、标的额、回款率）+ 累计口径（累计合同额/回款/回款率） |
| 二 | 历史年度对比 | 全量历史年度：新增案件、结案、新签合同额、回款、回款率，并附案件数/合同额/回款三项同比，带合计行 |
| 三 | 区间月度对比 | 区间内按 `YYYY-MM` 分组：新增、结案、新签合同额、回款、回款率，带合计行 |
| 四 | 项目进度 | 按案件 `project` 字段分组，空值归入「未分类项目」；含案件数、立项、结项、暂停、进行中、代理费、回款、回款率、结案进度条，带合计行 |
| 五 | 人员业绩 | 按 `dim` 指定口径分组（主承办人 / 开发人 / 小组成员）；列同项目维度，带合计行 |

### 9.3 时间区间筛选

| 控件 | 行为 |
|---|---|
| 起止日期 `dashStart` / `dashEnd` | `type="date"`，`onchange` 即时重新查询 |
| 快捷按钮 | 本月 / 上月 / 本季 / 本年 / 全部（全部为 `2000-01-01 ~ 2099-12-31`），由 `dashQuick(k)` 计算并触发 |
| 人员口径下拉 `dashDim` | 切换 `owner` / `developer` / `member`，即时重新查询 |
| 默认区间 | 首次进入若日期为空，自动取当月（`dashLoadDefaultRange()`） |

### 9.4 前端实现要点

- 新增渲染辅助函数：`dashQuick`（快捷区间）、`dashTable`（统一表格 + 合计行）、`dashBar`（进度条，复用 `.gantt-bar/.gantt-fill` 样式）、`dashFoot`（合计行，回款率按合计重新计算而非各行简单相加）、`dashTrend`（同比着色，正增红、负增绿）、`dashPct` / `dashMoney`。
- 复用原有 `ringSvg()` 绘制回款率环形图，未新增图表依赖。
- 空数据状态下五个板块均显示占位文案（如「当前区间无人员数据」），不做静默空白。
- 人员业绩表下方固定提示：代理费/回款按案件**全额计入各关联人员**（参与口径），故人员合计可能大于全所合计，避免用户误读为数据错误。

### 9.5 关联变更与遗留

| 项 | 说明 |
|---|---|
| 移除的内容 | 工作台「案件面板」卡片、`loadPanel()` 函数；`.panel-grid` / `.mini-stat` 等样式保留复用 |
| 遗留（暂不处理） | 后端 `/api/panel` 已无前端调用方，暂保留；`salary_rules` 表与 `/api/salary/rules` 同理，待统一清理 |
| 打包产物 | `dist/server/_internal/templates/index.html` 为旧构建产物，**未手工同步**；其中打包的 `app.py` 亦不含 `/api/dashboard`，需执行 `build.bat` 重新打包后方可生效 |

---

## 修订记录
| 版本 | 日期 | 说明 |
|---|---|---|
| v1.0 | 2026-08-28 | 初稿 |
| v1.1 | 2026-08-28 | 第七节按实际页面/代码逐条核对重写；新增 7.0 差异对照总表（D1–D8）与完整测试用例集（A/B/S/C 系列）；第八节排期表增加差异映射；清理文档尾部历史残留 |
| v1.2 | 2026-08-31 | 实施 P2 三项改造：D4 调整薪酬改模态框表单、D5"板块"全局更名"部门"、D6 暂停状态专属标签样式；同步更新差异总表与用例核对状态 |
| v1.3 | 2026-08-31 | 实施 P3 四项改造：B1 成员拖拽排序（/members/sync 全量重建持久化）、B2 案件类型下拉（新增 /api/case-types 只读接口 + settings.case_types 默认 20 类可配置）、B3 收费标准"人民币元"后缀、D7 搜索扩展至七字段（案件列表与数据信息库同步）；B1–B3 与 D7、D8 全部标记已实施 |
| v1.4 | 2026-08-31 | P1 收尾：第 3.1 节重写为单区域卡片展示（D1）、筛选维度更正为提成结算月（D2）、成员明细更正为 8 列（D3）；5.2 接口表与 5.3 返回结构同步修正为平坦列表；至此 D1–D8 全部闭环 |
| v1.5 | 2026-08-31 | 新增第九节「案件看板模块」：工作台内嵌「案件面板」升级为独立子页面「案件看板」（侧边栏第 2 项，权限 `case.view`）；新增 `/api/dashboard` 接口（参数 `start`/`end`/`dim`），5.2 接口表增行并新增 5.2.1 完整口径说明与返回结构；页面含指标卡、区间概览、历史年度对比（含同比）、区间月度对比、项目进度、人员业绩六大块，支持快捷键区间与人员口径切换；明确期间口径与期末口径的差异、人员参与口径的合计偏差提示，以及 `dist` 打包产物需重新构建的注意事项 |
*文档结束。如需调整或补充，请反馈。*
