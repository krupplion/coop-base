# 操作指引 · 结算单生成流程

> 适用对象：财务部财务专员 / 财务经理，以及需要生成或配置结算文档的操作人员。
> 系统入口：浏览器访问 `http://<服务器地址>:8100`（本机为 `http://127.0.0.1:8100`），默认账号 `admin / 123456`（首次登录后请立即修改密码）。
> 文档版本：v2.0（对应结算页合并为 2 模块 + 模板四部分结构的版本）

---

## 一、功能总览

结算页（菜单「结算单」）仅包含两个操作模块：

| 模块 | 用途 | 产出 |
|---|---|---|
| **① 一键生成结算文档** | 勾选案件 → 批量生成合并 Word 文档 | `结算文档_时间戳.zip`，内含每案一份 `（ORG_NAME）案件结案审批表.docx` |
| **② 结算模板管理** | 按 Word 模板四部分结构维护模板（含系统内置模板） | 模板配置；可下载空白模板 `案件结案审批表.docx` |

每案生成的合并 Word 文档为纸质单据的四段式结构：

| 部分 | 内容 | 来源 |
|---|---|---|
| **第1部分** 案件基本信息 | 原告 / 被告 / 板块 / 案由 / 标的 / 项目 / 收益标准 / 预期收益（8 字段） | 从案件管理自动带入，是否显示与顺序由模板配置 |
| **第2部分** 案件办结情况 | 费用收回明细（收入填正数、垫付填负数）+ 出纳审核意见 / 收益 / 备注 / 提成计算 + 维护人签字 | 系统自动生成：费用明细来自「费用明细录入」，收益与提成按公式自动计算 |
| **第3部分** 审批意见 | 维护人 / 财务主管 / 执行主任签字位 + 日期 | 姓名自动带入（维护人、负责人），**日期留白待手填** |
| **第4部分** 说明 | 固定 5 条说明 | 只读，不可修改 |

每个案件文件后按成员（小组成员）追加各人《薪酬结算单》，以分页符分隔，同一 Word 文件内合并输出。

---

## 二、前置条件与校验规则

### 2.1 账号与权限

| 权限点 | 对应操作 | 无权限时的表现 |
|---|---|---|
| `settle.view` | 查看模板列表、案件列表、费用明细 | 进入页面无数据或提示无权访问 |
| `settle.edit` | 新建/编辑/删除模板、录入费用明细、生成结算文档 | 操作按钮不显示或提交返回「无权限」 |

> 系统内置角色：管理员（admin）拥有全部权限；其余角色请由管理员在「权限管理」中分配。

### 2.2 数据准备（生成前必查）

生成一份可用的结算文档，需满足以下数据条件：

| # | 前置条件 | 说明 | 不满足时的处理 |
|---|---|---|---|
| 1 | 案件已录入且未删除 | 案号、客户名称、负责人等基本信息完整 | 到「案件台账」补齐后刷新结算页 |
| 2 | 案件有成员（小组成员 ≥ 1 人） | 无成员的案件不产出成员薪酬结算单部分 | 到案件编辑中维护「小组成员」 |
| 3 | 财务已确认回款金额（`confirmed_amount > 0`） | 「可结算」标记（settle_ready）由系统据此判断 | 在案件中确认回款金额后，系统自动更新可结算状态 |
| 4 | 费用明细已录入（可选但有意义） | 第2部分的费用收回明细与收益公式依赖它 | 可在模板抽屉「第2部分」录入 |

### 2.3 「可结算」判定规则（系统自动）

```
可结算（settle_ready）= 财务确认回款金额 confirmed_amount > 0  且  案件成员数 > 0
```

- 结算页案件列表中满足条件的案件显示「**可结算**」标签并**自动勾选**，未满足的显示「未达标」。
- 点击「勾选所有可结算案件」按钮可一键勾选全部达标案件。
- 手动勾选未达标案件也可生成文档（系统不阻止），但文档中收益/提成将按实际录入数据计算，请操作人员自行确认数据完整性。

### 2.4 模板校验规则（保存模板时）

| 校验项 | 规则 | 违反时提示 |
|---|---|---|
| 模板名称 | 必填 | 「请填写模板名称」 |
| 模板类型 | 仅支持 `salary_settlement` / `case_approval`（页面固定为审批表类型） | 后端自动规整 |
| 提成比例 | 承办人、维护人比例（默认 5% / 8%），保存后写入模板 | 需填写数字，0 表示不计算 |
| 系统内置模板 | **禁止删除**（按钮置灰禁用；接口返回 403） | 「系统内置模板禁止删除」 |
| 默认模板 | 同一时刻仅一个模板为默认（新设默认自动取消旧默认） | 无 |

---

## 三、全流程步骤总览

```
┌──────────┐   ┌──────────────┐   ┌──────────────┐   ┌──────────────┐   ┌────────────┐
│ 步骤1     │ → │ 步骤2         │ → │ 步骤3         │ → │ 步骤4         │ → │ 步骤5       │
│ 进入结算页 │   │ 选择/配置模板  │   │ 维护费用明细   │   │ 勾选案件并生成  │   │ 下载与核对   │
│（登录后）  │   │（模块②或下拉） │   │（模板第2部分） │   │（模块①）      │   │（ZIP→Word） │
└──────────┘   └──────────────┘   └──────────────┘   └──────────────┘   └────────────┘
```

| 步骤 | 操作位置 | 核心动作 | 耗时参考 |
|---|---|---|---|
| 1 | 左侧菜单「结算单」 | 进入结算页，确认模块①②与案件列表已加载 | 即时 |
| 2 | 页面顶部「结算模板」下拉 或 模块② | 选择默认模板，或新建/编辑模板 | 新建约 1–3 分钟 |
| 3 | 模板抽屉「第2部分」 | 选择预览案件 → 录入费用明细（可选） | 按明细条数 |
| 4 | 模块① | 勾选案件 → 点击「一键生成结算文档」 | 数秒 |
| 5 | 浏览器下载目录 | 解压 ZIP，逐份核对 Word 内容 | 按案件数 |

---

## 四、详细操作步骤

### 步骤 1：进入结算页

1. 打开浏览器，访问系统地址并登录。
2. 点击左侧菜单「**结算单**」，页面加载后应显示：
   - 模块①「一键生成结算文档（薪酬结算单 + 案件结案审批表合并）」：含结算模板下拉、案件勾选表格、「一键生成结算文档」按钮；
   - 模块②「结算模板管理（自定义模板）」：模板列表 + 「+ 新建模板」按钮；
   - 下方「历史结算记录」列表（只读）。

### 步骤 2：选择 / 配置结算模板

**方式 A：直接选用默认模板（最快）**

- 页面顶部「结算模板」下拉框已**默认选中默认模板**（标记「（默认）」）。
- 若只需生成文档，跳到步骤 4 即可。

**方式 B：新建自定义模板（模块② → 「+ 新建模板」）**

打开四部分结构编辑抽屉，按以下顺序配置：

**第1部分 · 案件基本信息**
- 8 个字段（原告 / 被告 / 板块 / 案由 / 标的 / 项目 / 收益标准 / 预期收益）默认全部勾选。
- 取消勾选 = 生成文档时不显示该行；点击字段右侧「↑ / ↓」可调整在文档中的显示顺序。
- 操作要点：只需勾选该案件需要展示的信息，无关字段取消勾选可使文档更简洁。

**第2部分 · 案件办结情况（费用明细 + 提成比例）**
- 「选择案件」下拉选择要预览/录入费用明细的案件（**该下拉仅用于预览与录入，不写入模板本身**）。
- 录入费用明细：填写「日期 / 款项说明 / 金额」→「+ 添加费用明细」。**收入填正数、垫付填负数**；负数金额在表格中以红色显示。
- 每条明细实时累加，右侧「收益计算 / 提成预览」区域即时更新：
  - 收益 = 所有正数之和 − 所有负数绝对值之和；
  - 提成预览 = 承办人比例 × 收益、维护人比例 × 收益（默认 5% / 8%，可改）。
- 操作要点：添加/删除明细会**立即保存到该案件**（不是临时预览）；生成文档时读取的是案件已保存的全部费用明细。

**第3部分 · 审批意见（只读预览）**
- 展示签字位版式：维护人 / 财务主管 / 执行主任 + 日期位。
- 生成文档时姓名自动带入（维护人取案件维护人、财务主管/执行主任留空），**日期一律留白（`____年__月__日`）待手填**——与预览显示一致。

**第4部分 · 说明（固定只读）**
- 5 条固定说明不可编辑、不可删除，生成文档时原样输出。

**合并薪酬结算单配置（可选）**
- 「显示批次号」「显示结算日期」「显示结算人员」三个开关控制合并文档中结算单部分的元信息行；
- 「结算单显示字段」勾选案号 / 客户名称 / 案件开发人 / 负责人 / 小组成员 / 代理费金额 / 代垫诉讼费 / 开票金额等；
- 「落款」填写结算单底部签字文字（默认：财务经理签字：____  领款人签字：____）。

**保存 / 预览**
- 「预览 Word」：按当前未保存的配置立即生成一份预览文档下载（未选择案件时第1部分字段留空）。
- 「保存模板」：必填「模板名称」后保存；保存后列表更新。
- 「取消」：放弃本次编辑关闭抽屉。

**方式 C：编辑已有模板**

- 在模块②列表中点击「编辑」打开抽屉，配置项与新建一致，保存后覆盖原模板。
- 列表操作列说明：
  - 「设为默认」：将该模板设为默认（页面下拉自动选中）；
  - 「删除」：**系统内置模板的删除按钮置灰禁用**，仅自定义模板可删除（删除前有确认提示）。

**空白模板下载**
- 模块②模板列表 → 任意模板行操作列可下载对应空白版式；也可在模板编辑抽屉中通过「预览 Word」获取空白样式文档。

### 步骤 3：维护费用明细（可选，但影响第2部分内容）

1. 在模板抽屉「第2部分」的「选择案件」下拉中选中目标案件（也可直接在结算页生成前完成）。
2. 添加明细：填写日期（如 `2026-07-15`）、款项说明（如 `代理费` / `垫付担保费`）、金额（正数=收入，负数=垫付），点击「+ 添加费用明细」。
3. 删除明细：点击明细行右侧「删除」。
4. 确认右侧「收益计算 / 提成预览」数字与预期一致（可先核对公式：正数相加、负数绝对值相减）。

> 费用明细数据源为案件级（`case_fee_detail` 表），与模板无关；同一案件无论用哪个模板生成，第2部分费用明细均一致。

### 步骤 4：一键生成结算文档（模块①）

1. 在页面顶部「结算模板」下拉中确认模板（默认已选默认模板；如需其他模板请先切换）。
2. 在案件勾选表格中勾选本次要生成的案件（可结算案件默认已勾选；「勾选所有可结算案件」可一键补选）。
   - **校验规则：必须至少勾选 1 个案件**，否则点击生成会提示「请勾选案件」。
3. 点击「**一键生成结算文档（薪酬结算单 + 案件结案审批表合并）**」按钮。
4. 页面出现半透明 loading 遮罩「正在生成结算文档...」，生成期间请勿重复点击。
5. 生成完成后浏览器自动下载 `结算文档_YYYYMMDDHHmmss.zip`（时间戳为服务器生成批次号），提示「已生成 N 份（ORG_NAME）案件结案审批表，开始下载」。

### 步骤 5：下载与核对

1. 解压 ZIP，确认文件清单：
   - 勾选 1 个案件 → 压缩包内 1 份文件：`（ORG_NAME）案件结案审批表.docx`；
   - 勾选多个案件 → 每案 1 份：`案号_（ORG_NAME）案件结案审批表.docx`（如 `#260901_（ORG_NAME）案件结案审批表.docx`）。
2. 用 Word/WPS 打开文件，核对四部分：
   - 第1部分基本信息是否与案件一致（字段是否按模板勾选/排序显示）；
   - 第2部分费用明细条数与金额、收益公式（`本案收益为 X -Y -Z=W 元`）、提成金额（承办人 5%、维护人 8%）是否正确；
   - 第3部分签字位姓名、日期留白是否就位；
   - 第4部分 5 条说明是否齐全；
   - 各成员《薪酬结算单》是否按成员逐一输出、金额与提成一致。
3. 如需补录日期/签字，直接在 Word 中手填即可（日期位置已预留下划线空白）。

---

## 五、校验规则汇总

### 5.1 前端（页面即时校验）

| 场景 | 校验规则 | 提示信息 |
|---|---|---|
| 一键生成 | 至少勾选 1 个案件 | 「请勾选案件」 |
| 添加费用明细 | 已选择案件 | 「请先选择案件（第2部分「选择案件」下拉）」 |
| 添加费用明细 | 款项说明非空 | 「请填写款项说明」 |
| 添加费用明细 | 金额为数字 | 「请填写金额」 |
| 保存模板 | 模板名称非空 | 「请填写模板名称」 |
| 删除模板 | 系统内置模板 | 按钮置灰禁用（不可点击） |

### 5.2 后端（接口强制校验，前端绕过时兜底）

| 接口 | 校验规则 | 返回 |
|---|---|---|
| `POST /api/generate-settlement` | `case_ids` 为空 | `400 请勾选案件` |
| `POST /api/generate-settlement` | 所有案件均不存在/已删除 | `400 所选案件未生成任何文档` |
| `DELETE /api/templates/{id}` | 模板为 `is_system=1` | `403 系统内置模板禁止删除` |
| `PUT/DELETE /api/templates/{id}` | 模板不存在 | `404 模板不存在` |
| `POST /api/templates` | 名称缺失 | `400 请填写模板名称` |
| 费用明细相关 | 案件不存在 | `404 案件不存在` |
| 全部写操作 | 无 `settle.edit` 权限 | `403 无权限` |

---

## 六、异常情况与处理方式

| # | 现象 | 原因 | 处理方式 |
|---|---|---|---|
| 1 | 点击生成提示「请勾选案件」 | 未勾选任何案件 | 勾选至少 1 个案件后重试 |
| 2 | 提示「所选案件未生成任何文档」 | 勾选的案件在生成时已不存在或已删除 | 回到案件台账核对案件状态，重新勾选 |
| 3 | 生成时长时间停留在遮罩、无下载 | 服务器繁忙或网络中断 | 等待片刻；若超 1 分钟仍无响应，刷新页面重试；仍失败请联系管理员查看服务器日志 |
| 4 | 提示「生成失败，请稍后重试」 | 服务异常或会话过期 | 重新登录后重试；若重复出现，联系管理员检查 `data/` 目录权限与磁盘空间 |
| 5 | 模板下拉为空或没有默认模板 | 数据库中无默认标记模板 | 到模块②点任意模板「设为默认」（系统内置模板默认已为默认） |
| 6 | 无法删除某模板（按钮置灰） | 该系统内置模板，受保护 | 系统内置模板不可删除；如需类似配置，请「新建模板」复制一份自定义模板 |
| 7 | 编辑模板提示「模板不存在」 | 模板已被他人删除或数据异常 | 刷新列表后重新操作；必要时重建模板 |
| 8 | 费用明细无法添加（提示先选案件） | 「第2部分」未选择预览案件 | 先在「选择案件」下拉选中目标案件 |
| 9 | 收益/提成预览数字异常 | 明细金额正负号录反，或比例填写有误 | 检查每条明细符号（收入正、垫付负），检查承办人/维护人比例 |
| 10 | 生成的 Word 中第2部分无费用明细 | 该案件未录入费用明细（模板保存的是配置，不是明细） | 在模板抽屉第2部分选择该案件并录入明细后重新生成 |
| 11 | 生成的 Word 中第3部分日期为空白下划线 | 属**预期行为**：日期位置已改为留白，供手填 | 直接在手写/电子文档中填写日期即可，无需处理 |
| 12 | 生成的 Word 中成员薪酬结算单缺失 | 该案件未维护「小组成员」 | 到案件编辑中维护成员后重新生成 |
| 13 | 下载的 ZIP 里没有某案件的文件 | 该案件在生成时已被删除或不可用 | 核对案件状态后重新生成 |
| 14 | 登录后结算页无数据 | 无 `settle.view` 权限 | 联系管理员在权限管理中分配 |
| 15 | 保存模板提示「无权限」 | 当前账号无 `settle.edit` 权限 | 联系管理员分配权限 |

---

## 七、关键规则说明

1. **收益计算公式**（第2部分）
   ```
   收益 = Σ(正数金额) − Σ(负数金额的绝对值)
   ```
   文档中展示完整算式，如：`本案收益为 52000.00 -3000.00 -2000.00=47000.00 元`。

2. **提成计算公式**（第2部分）
   ```
   承办人提成 = 收益 × 承办人比例（模板默认 5%）
   维护人提成 = 收益 × 维护人比例（模板默认 8%）
   ```
   金额按标准四舍五入（ROUND_HALF_UP）保留 2 位小数，规避银行家舍入误差。比例可在模板第2部分修改后保存。

3. **文件命名规则**
   - ZIP：`结算文档_YYYYMMDDHHmmss.zip`（批次号即时间戳）；
   - 单案：`（ORG_NAME）案件结案审批表.docx`；
   - 多案：`案号_（ORG_NAME）案件结案审批表.docx`。

4. **日期留白规则**：审批意见签字日期、薪酬结算单「结算日期」均不自动填充，输出 `____年__月__日` 下划线占位，供手填；批次号（流水号）与费用明细中的日期（用户录入数据）不受影响。

5. **系统内置模板保护**：`(ORG_NAME)案件结案审批表` 为系统内置（`is_system=1`），可编辑、可设为默认，但**禁止删除**（前端置灰 + 后端 403 双重防护）。

6. **默认模板唯一性**：同一时刻只有一个模板为默认；新设置默认模板时，旧默认自动取消。

7. **费用明细数据归属**：费用明细保存在**案件**名下（非模板名下），同一案件用任意模板生成，第2部分明细一致；模板只保存字段勾选、顺序、比例与结算单配置。

8. **预览与生成的关系**：「预览 Word」使用当前抽屉中尚未保存的配置立即生成一份文档（供检查版式）；若未选择案件，第1部分字段为空白占位。正式生成请使用模块①。

---

## 八、常见问题（FAQ）

| 问题 | 解答 |
|---|---|
| 生成的文档里日期为什么是空的？ | 这是上一轮迭代的刻意设计：日期位置改为留白（`____年__月__日`），由签批人手填，避免自动日期与实际签批日不符。 |
| 如何让某案件不显示「原告/被告」等字段？ | 编辑模板 → 第1部分取消勾选对应字段 → 保存；生成时选择该模板即可。 |
| 某案件没有收益/提成，只有费用明细？ | 收益由费用明细正负计算得出；若案件无任何明细，第2部分显示「本案收益为 ____ 元（暂未录入费用明细）」，提成按 0 计算。 |
| 可以同时给不同案件用不同模板吗？ | 可以分两批：切换模板下拉 → 勾选对应案件 → 生成，重复操作即可。 |
| 生成后想微调个别字段怎么办？ | 直接在下载的 Word 中编辑即可；文档不受系统数据影响。 |
| 如何下载空白模板？ | 模板列表 → 对应模板行「下载」按钮，或使用抽屉「预览 Word」（未选案件时即空白版式）。 |
| 模板被我改乱了能恢复吗？ | 系统内置模板可通过重新编辑修正；也可「新建模板」重建自定义模板。重要配置修改前建议先记录原值。 |

---

*本指引随系统迭代更新；如与界面实际不符，以系统当前版本为准。*
