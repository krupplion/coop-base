# APP_DATA_NAME · 操作人员操作指引

> 适用对象：财务部「财务专员 / 财务经理」、案件「案件专员」等日常录入与核对人员。
> 系统形态：财务部电脑作为局域网服务器，其他人员通过浏览器访问；数据均存储在本地，不外传。
> 文档版本：v1.2（在 v1.1 基础上增补「案件停止 / 启用 / 删除」操作说明）

---

## 一、登录与基础操作

1. 打开浏览器访问服务器地址（默认 `http://127.0.0.1:8100`，局域网内为服务器局域网 IP）。
2. 输入用户名 / 密码登录（默认账号 `admin / 123456`，**首次登录后请立即修改密码**）。
3. 左侧为功能导航，点击即可切换页面；所有列表页支持搜索框实时筛选，回车或点击「查询」生效。
4. 重要操作（新增 / 修改 / 删除 / 确认）均写入审计日志，可于「审计日志」页面追溯。

---

## 二、各模块操作要点

### 2.1 案件台账
- 新增案件：点击「+ 新增案件」，录入案号（默认自动生成 `CG年份-序号`，可改）、客户名称、负责人、案件类型、代理费、开票金额等。
- 编辑 / 状态流转：在列表中点击「编辑」，或拖动状态卡片流转（仅允许合法状态顺序）。
- 每个案件可维护「开发人 / 负责人 / 小组成员」「原告 / 被告 / 案由」「回款与提成结算月」等字段。

### 2.2 人员花名册
- 维护人员工号、姓名、部门、岗位、入职日期、联系电话、身份证号与「案件提成率」。
- 提成率会被薪酬计算自动引用，录入时请与财务确认口径一致。

### 2.3 客户信息
- 维护客户名称、统一社会信用代码、联系人、电话、客户类型、所属行业、地址。
- 统一社会信用代码用于开票与对账，请准确录入。

### 2.4 发票识别（台账）
- **发票批量导入（推荐）**：在「发票批量导入」卡片一次选择多张发票（图片 JPG/PNG/BMP 与 PDF 可混传），逐张自动识别并登记台账；结果按「成功 / 缺字段待补 / 失败」三态逐文件展示，单张异常不中断整批，可填公共案件号统一登记。
- **解析引擎**：文本型电子发票 PDF 由 pdfplumber 精确提取文本层（金额、号码零误差），扫描件自动转图后由本地 RapidOCR 识别，**全程数据不出本机**。本地引擎同样按规范口径输出字段：购买方 / 销售方名称（含"名 称："字间空格写法）、双方税号、项目名称（含 `*编码*` 前缀）、价税合计（小写+大写，存入 `价税合计` 列）、税率 / 征收率（支持明细行裸值与标签式）、开票人、备注。
- **自动识别字段**：发票类型（输出票面完整标题，如「电子发票（增值税专用发票）」；旧数据兼容「专票 / 普票」简称）、发票号码、**购买方（即客户名称，识别为完整企业全称）**、**销售方全称及双方税号**、**项目名称（完整保留 `*税收分类编码*` 前缀）**、金额（不含税）、税额、**价税合计（小写+大写）**、**税率 / 征收率**、开票人、备注、开票日期；费用科目优先取项目名称（去除 `*税收分类编码*` 前缀，如 `*生产生活服务*法律咨询` → `法律咨询`），无项目名称时按关键词归集。
- **AI 识别规范（与《发票AI识别提示词（整合版）》完全一致）**：① 购买方 / 销售方名称必须输出完整企业全称（行政区划 + 字号 + 行业 / 组织形式），禁止简称、漏字，名称与税号必须同源；② 项目名称必须原样保留税收分类编码前缀与星号（OCR 把星号误识为 × / x / 空格时按上下文还原为 `*`），多行应税项目用「；」分隔；③ 识别后自动执行勾稽校验（金额 + 税额 = 价税合计、金额 × 税率 ≈ 税额、大写与小写一致），任一不通过会在备注追加「【勾稽异常：请人工复核】」。
- **系统二次校验（双保险）**：识别结果入库时再次校验——名称疑似 2~6 字简称、项目名称缺失 `*编码*` 前缀、金额勾稽异常，均自动标记「待核对」并在编辑弹窗提示原因，请人工复核补正。
- **异常处理**：识别失败（无任何发票要素 / 文件损坏）不入库并提示原因；缺发票号码 / 购买方 / 价税合计 / 开票日期等关键字段的发票仍会登记，但标记「待核对」并逐项提示缺失字段，可在编辑弹窗补录。
- **单张识别（备用）**：也可用「本地离线 OCR 识别」或「AI 电子发票识别（可选，需在系统设置 → AI 接口配置服务商）」单张上传识别。
- **手动新增**：点击「+ 新增发票」，填写案件号、发票号码、**购买方（客户名称，企业全称）**、发票类型（专票 / 普票 / 完整票面标题均可）、项目名称（含 `*编码*` 前缀）、费用科目、金额（不含税）、税额、**价税合计、税率**、开票日期、双方税号、开票人、备注，提交即登记入账。
- 列表支持「按案件号」「按购买方」筛选；金额口径与发票规范一致——列表「价税合计」列展示票面价税合计（旧数据已自动迁移），并按发票类型（专票红标）与项目名称展示，便于核对与分类统计。
- 识别 / 录入后请人工「确认」，确认后的发票参与开票金额比对与结算；销售方识别与默认「ORG_NAME」不一致（不含包含关系）、名称疑似简称、项目名称缺前缀或勾稽异常的发票会带「待核对」标记。

### 2.5 薪酬计算
- 基于「薪酬规则」+「案件回款 / 开票金额」自动计算提成，结果展示完整计算过程与公式（基数 × 比例 = 精确值 → 四舍五入取整）。
- 可自定义公式（如 `agent_fee*0.1 + invoice_amount*0.02`）或固定金额；支持按结算月筛选。

### 2.6 结算单
- 结算页仅两个模块：**① 一键生成结算文档**（勾选案件 → 每案生成合并 Word《（ORG_NAME）案件结案审批表》（四部分结构 + 按成员薪酬结算单）→ 打包 ZIP 下载）、**② 结算模板管理**（按四部分结构配置模板，系统内置模板禁删）。
- 生成前请确认：案件已确认回款金额且有小组成员（「可结算」标签）；费用明细收入填正数、垫付填负数；日期位留白手填。
- **完整流程、前置条件、校验规则与异常处理详见《结算单生成流程操作指引.md》**（项目根目录，随系统同步更新）。

### 2.7 数据自动备份
- 入口：**系统设置 → 数据自动备份**（需「设置管理」权限，通常为管理员/财务经理）。
- **备份内容**：单次备份打包为一个 ZIP，含 ①数据库（在线快照，无需停机）②上传文件（发票图片/PDF）③结算模板 ④配置文件（AI 服务商配置），并附 `manifest.json` 清单。
- **配置项**：启用开关、备份路径（留空 = 默认 `data/backups`，建议填其他磁盘如 `D:\ORG_NAME备份`）、执行周期（每日/每周/按间隔/仅手动）、触发时间、每周几、间隔小时、保留份数（0 = 不清理）、备份范围勾选。
- **立即备份**：点击「立即备份」即异步执行，页面显示进度条与阶段提示，完成后显示"上次备份成功 + 文件/大小/耗时"。
- **备份列表**：展示全部备份文件，支持「下载」（保存到本机）与「删除」（单个删除）。
- **过期清理**：系统在每次备份成功后按「保留份数」自动清理最旧的备份；也可点「清理过期备份」手动触发。
- **日志**：备份页底部展示最近 200 行日志（成功/失败/清理均记录），失败时给出可读原因（如路径无效、无写入权限、磁盘空间不足、文件被占用）。
- **建议**：首次使用先「立即备份」一次验证路径可写；每周至少保留 1 份异盘备份，防止同盘损坏。

### 2.8 用户与权限管理
入口：**用户权限**页面（需「用户管理」权限，通常为管理员 / 财务经理）。
- **新建用户（可创建时直接分配权限）**：
  1. 填写用户名、姓名、初始密码（默认 `123456`，创建后请提醒其及时修改）、选择角色（财务经理 / 财务专员 / 案件专员 / 查看人员 / 机构负责人 / 律师）。
  2. 点击「**权限配置**」按钮，在弹窗中勾选该用户应拥有的具体权限（权限按模块分组，显示中文标签 + 权限键）；全部留空表示**继承所选角色的默认权限**。确定后表单下方显示已配置权限数摘要。
  3. 点击「新建用户」提交；若用户名已存在、角色非法或权限键非法，将提示具体错误（如「用户名已存在」）。
- **修改用户权限**：在列表行点击「**权限**」按钮，打开同一弹窗调整并保存，立即生效。
- **停用 / 启用**：行内「停用」按钮将账号置为停用（无法登录，数据保留），可再次启用。
- **删除用户（二次确认 + 数据清理）**：行内「删除」按钮，需**连续两次确认**（提示「不可恢复」）。删除后将移除该账号，并自动清理其**个人薪酬规则**与**账号生命周期审计记录**；其经手的案件 / 结算单 / 操作日志等历史业务数据**予以保留**，确保可追溯。**内置管理员 `admin` 不可删除**。
- **角色与权限说明**：财务经理 = 最高权限（全部数据 + 用户 + 系统配置）；财务专员 = 案件财务字段 + 薪酬 + 结算单 + 发票；案件专员 = 案件基本信息录入；查看人员 = 只读；机构负责人 = 机构全局管理 + 审批结案 + 管理本机构人员；律师 = 案件基本信息录入与维护。各角色可拥有的权限还可在「系统设置 → 人员角色权限管理」中调整（仅修改「谁能做什么」，不修改角色名）。

> 注意：用户级权限（创建 / 修改时分配）**优先于角色默认**；留空即继承角色。删除用户不可恢复，请务必二次确认；如仅暂时离岗，优先使用「停用」。

### 2.9 案件停止 / 启用 / 删除
入口：**案件台账**列表（权限按钮随当前账号权限自动显隐）。
- **停止案件**（需「停止案件」权限，通常为财务经理 / 财务专员 / 机构负责人）：对启用中的案件点击「**停止**」，确认后该案件标记为「已停止」（状态列显示红色「已停止」标签），数据完整保留。适用于案件暂停办理、暂不推进等场景。
- **启用案件**（需「启用案件」权限，同上）：对「已停止」的案件点击「**启用**」，确认后恢复为正常启用状态，可继续录入与流转。
- **删除案件**（需「删除案件」权限，通常为财务经理 / 机构负责人）：点击「**删除**」，确认后案件进入软删除（列表不再显示，仅留审计日志可追溯）。
- **权限联动**：上述三类操作各自受独立权限控制——「停止案件」「启用案件」「删除案件」可在「系统设置 → 人员角色权限管理」中分别授予不同角色；无对应权限的账号**看不到对应按钮**，点击亦不会生效。
- **停用 vs 删除**：「停止」只是停用该案件（可再次启用），**不破坏任何数据**；「删除」是软删除，虽保留审计痕迹但案件将退出日常列表。如仅暂时离岗 / 暂停，优先用「停止」而非「删除」。

> 注意：案件「停止 / 启用」仅改变启用状态，**不影响案件既有状态（如一审 / 结案）与所有业务数据**；如需改变案件所处阶段，请用「编辑」或看板拖动进行状态流转。

---

## 三、测试数据保留要求（重要）

### 3.1 要求
- 系统已为**每个主页面预置 3 条演示测试数据**，操作人员**必须予以保留，不得执行任何删除操作**。
- 本指引、培训材料、演示场景中，**均不得要求或建议删除测试数据**；即便测试数据被误改，也以「恢复 / 重新灌入」处理，而非删除。

### 3.2 保留原因
1. **培训与演练安全**：新同事可在结构真实的数据上练习录入、识别、流转、结算，避免直接操作生产数据带来的误删 / 误改风险。
2. **功能回归验证**：用于验证各模块（OCR / AI 识别、费用科目归集、按案件号 / 购买方筛选、薪酬计算与取整、结算单生成）是否正常。
3. **演示与汇报**：对外演示系统时无需临时造数，开箱即有可展示内容。
4. **可明确区分**：全部测试数据均以 **`DEMO` 字样**标识（案号 `DEMO2609xx`、客户信用代码含 `DEMO`、发票号 `2441700000000000000x`），与真实数据界限清晰，可在筛选时排除，不影响真实统计。
5. **数据安全兜底**：保留测试数据可避免在生产库中误删真实记录；测试数据本身不含任何真实客户敏感信息。

---

## 四、操作人员使用测试数据的具体步骤

1. **登录系统**（默认 `admin / 123456`，首次登录修改密码）。
2. **进入对应模块**，可见已预置的 3 条测试数据，例如：
   - 案件台账：`DEMO260901 示例科技（北京）有限公司`、`DEMO260902 宏远建设工程有限公司`、`DEMO260903 个人客户刘某某`；
   - 人员花名册：`S001 张明`、`S002 李华`、`S003 王芳`；
   - 客户信息：`示例科技（北京）有限公司`、`宏远建设工程有限公司`、`金海贸易有限公司`（信用代码含 `DEMO`）；
   - 发票识别：`24417000000000000001 / 002 / 003`，购买方分别对应上述客户，且已关联 DEMO 案号；
   - 薪酬规则：`张明`（公式）、`李华`（公式）、`王芳`（固定金额）。
3. **演练录入**：在测试案件上练习「新增 / 编辑 / 状态流转」；在发票模块用测试发票演练「本地 OCR」「AI 识别」「手动新增」与「确认」。
4. **验证筛选**：在发票台账分别用「按案件号」输入 `DEMO260901`、用「按购买方」输入 `示例科技` 进行筛选，确认可精准定位到对应测试数据。
5. **薪酬演练**：在薪酬计算模块选择测试人员 / 测试案件，查看提成计算过程（含公式与取整说明），核对金额。
6. **自行练习造数**：如需在真实录入前另建练习数据，请在新增时同样加 `DEMO` 或明确标注，便于与真实数据区分与后续清理。
7. **恢复测试数据**：若测试数据被误改，可重新执行演示数据脚本（或联系管理员）恢复，**切勿手动删除后重建**。

---

## 五、注意事项

- ⚠️ **测试数据仅供内部培训、验证、演示使用，严禁作为真实业务依据**，不得用于正式结算单生成或对外报送。
- ⚠️ 导出 / 打印 / 生成结算单前，请确认筛选条件，**避免把 `DEMO` 测试数据混入真实报表**。
- ⚠️ 如确需在生产环境清空演示数据，须经主管确认并先备份数据库，再操作；**本指引默认保留**。
- 涉及密钥 / AI 服务商配置等敏感信息，按「系统设置 → AI 接口」要求通过**环境变量或本地配置文件**注入，本指引与界面均不会明文展示完整密钥。
- 多人协同录入时，注意「案件状态」流转顺序与字段级合并规则；冲突字段以最后写入为准。
- 服务器电脑请保持开机与局域网连通；修改密码、定期备份 `data/` 目录是日常运维要点。

---

## 六、常见问题（FAQ）

| 问题 | 处理 |
| --- | --- |
| 测试数据被误改了怎么办？ | 不要删除，重新执行演示数据恢复脚本即可，原有真实数据不受影响。 |
| 测试数据和真实数据怎么区分？ | 看 `DEMO` 标识：案号、信用代码、发票号含 `DEMO` 即为测试数据。 |
| 能否删除某条测试数据？ | 不允许。保留测试数据是操作规范，删除会破坏培训 / 验证环境。 |
| 筛选找不到某条发票？ | 确认「按案件号 / 按购买方」输入准确；也可在关键字框用发票号模糊搜索。 |
| AI 识别提示未配置？ | 到「系统设置 → AI 接口」选择服务商并填写密钥（环境变量 / 配置文件方式），保存后重试。 |
| 能否给某个用户单独加权限（不升级角色）？ | 可以。新建或编辑用户时点「权限配置」，勾选所需权限即可，无需改角色；该用户级权限优先于角色默认。 |
| 删除用户后数据还在吗？ | 账号被移除，其个人薪酬规则与账号审计被清理；经手的案件 / 结算单 / 日志保留可追溯。如仅暂时离岗，建议用「停用」而非「删除」。 |

---

*本指引随系统迭代更新；如与界面实际不符，以系统当前版本为准。*
