# 律所合伙人运营管理系统 — 提示词组

> 使用说明：以下三组提示词分别用于文生图、数据模型设计、前端实现，可直接交给对应岗位或 AI 工具执行。表述保持中文，意图完整保留。

---

## 提示词 1：文生图 — 律所运营看板桌面端示意图

生成一张**横版桌面端（16:9）律所合伙人运营看板 UI 示意图**，用于 PPT 汇报、需求评审或前端视觉参考。

### 整体风格
- 背景：浅灰白（#F5F7FA / #FFFFFF）干净底色，带极淡卡片投影。
- 设计语言：现代扁平化，圆角 8–12px，留白充足，信息层级清晰。
- 主色：深蓝（#1E3A5F / #2563EB），用于顶部导航、重点数字、柱状图。
- 强调色：金橙色（#F59E0B / #D97706），用于高亮、进度条、异常提示。
- 状态色：办结/正向用绿色（#10B981），异常/逾期用红色（#EF4444），终止/灰停用灰（#9CA3AF），普通流程用蓝色（#3B82F6）。

### 顶部导航栏
- 左侧：天平/法槌图标 + “律所运营看板”标题。
- 右侧横向导航：`运营看板` | `案件管理` | `回款管理` | `执行管理` | `报表统计`。
- 当前选中 `运营看板`，使用深蓝底色 + 白色文字高亮；其余为灰色文字 hover 变蓝。
- 最右侧：用户头像或“合伙人”角色标签。

### 第一行 KPI 卡片（5 张等宽卡片）
从左到右：
1. **本月新收案**：大号数字 `12`，下方小字“环比 +3 件”。
2. **在办案件**：大号数字 `45`，下方小字“非 closed 状态”。
3. **本月回款**：大号数字 `86.5万`，绿色 `↑12%` 增长率。
4. **未回款应收**：大号数字 `120.3万`，红色标注“逾期 18.2万”。
5. **综合回款率**：大号数字 `71.6%`，右侧小环形图 71.6% 蓝色填充。

每张卡片顶部左侧小标题，右上角可展开/详情的小图标；卡片白底、细边框、轻微悬浮感。

### 第二行布局
左侧占 60%，右侧占 40%。

**左侧：案件阶段看板（Kanban）**
- 标题“案件阶段看板”。
- 5 列横向排列，列标题分别为：
  - 咨询洽谈（蓝色标签）
  - 已签约委托（橙色标签）
  - 立案审理（深蓝标签）
  - 强制执行（金橙标签）
  - 已结案归档（绿色标签）
- 每列下方堆叠 2–3 张案件卡片，卡片内容：
  - 左：承办律师圆形头像或首字母。
  - 中：案件名（如“劳动争议案”“合同纠纷案”）+ 当事人简称。
  - 右：状态小圆点（灰/橙/绿）或金额标签。
- 列与列之间有拖拽视觉暗示（虚线边框或拖拽手柄）。

**右上：本月回款进度柱状图**
- 标题“本月回款进度”。
- 横轴：第 1 周 / 第 2 周 / 第 3 周 / 第 4 周。
- 纵轴：金额（万元）。
- 深蓝柱体，柱顶标注达成率（如 92%、78%、65%、58%、43%、31%）。
- 右上角用绿色小字标注总目标达成率 `31%`。

**右下：执行案件进度列表**
- 标题“执行案件进度”。
- 列表项（每条一行，左文字右进度条）：
  - 执行立案 5 件 —— 蓝色进度条
  - 执行中 12 件 —— 金橙进度条
  - 已执行到位 8 件 —— 绿色进度条
  - 终本 3 件 —— 灰色进度条
- 进度条长度与案件金额/数量成正比，直观展示占比。

### 输出要求
- 生成 1 张高保真 UI 概念图，无真实人物面部，无水印。
- 图中所有文字为中文，字体清晰可读。
- 画面整体专业、稳重、符合律所品牌调性。

---

## 提示词 2：数据模型与枚举 — 案件主状态 + 执行状态

为律所合伙人运营管理系统设计案件状态枚举，需同时输出 **TypeScript 前端联合类型**与**后端枚举类**（Python 可用 `Enum` / Java 可用 `enum`），要求每个状态包含中文 label、颜色标识、是否可向前端展示，以及**可流转状态约束**。

### 一、案件主状态 `case_status`
用于描述案件从线索到归档的全生命周期。

| 编码 | 中文 label | 颜色 | 分类 | 可流转至 |
|---|---|---|---|---|
| `pending_contact` | 待接洽 | 蓝 #3B82F6 | 正常流转 | `consulting` |
| `consulting` | 咨询评估中 | 蓝 #3B82F6 | 正常流转 | `consulted`, `contracting`, `rejected_conflict` |
| `consulted` | 已咨询待签约 | 蓝 #3B82F6 | 正常流转 | `contracting`, `rejected_fee`, `rejected_conflict` |
| `contracting` | 已签约待收案 | 蓝 #3B82F6 | 正常流转 | `filing_prepare`, `mediation_closed`, `withdrawn` |
| `filing_prepare` | 立案准备中 | 蓝 #3B82F6 | 正常流转 | `first_instance`, `mediation_closed`, `withdrawn` |
| `first_instance` | 一审 | 蓝 #3B82F6 | 正常流转 | `second_instance`, `execution`, `closed`, `mediation_closed`, `withdrawn`, `dismissed` |
| `second_instance` | 二审 | 蓝 #3B82F6 | 正常流转 | `execution`, `closed`, `mediation_closed`, `withdrawn`, `dismissed` |
| `execution` | 执行 | 金橙 #F59E0B | 正常流转 | `closed`, `execution_ended` |
| `closed` | 结案 | 绿 #10B981 | 结果类 | `archived` |
| `archived` | 归档 | 绿 #10B981 | 结果类 | — |
| `mediation_closed` | 调解结案 | 绿 #10B981 | 结果类 | `archived` |
| `withdrawn` | 撤诉 | 灰 #9CA3AF | 终止类 | `archived` |
| `dismissed` | 驳回 | 灰 #9CA3AF | 终止类 | `archived` |
| `terminated` | 终止 | 灰 #9CA3AF | 终止类 | `archived` |
| `rejected_conflict` | 拒接（利益冲突） | 红 #EF4444 | 异常类 | — |
| `rejected_fee` | 拒接（费用未达成） | 红 #EF4444 | 异常类 | — |

**分类说明**：
- 正常流转：蓝色系，案件仍在推进。
- 结果类：绿色系，案件取得结果但未归档。
- 终止类：灰色系，案件非正常结束。
- 异常类：红色系，案件未进入正式流程即结束。

**可流转约束规则**：
1. 结果类/终止类/异常类状态不可反向回到正常流转状态（归档除外）。
2. `closed` 必须先经过 `first_instance` 或 `second_instance` 或 `execution`。
3. `execution` 只能从 `first_instance` / `second_instance` 进入。
4. 任何非归档状态均可进入 `terminated`（需填写终止原因）。

### 二、执行状态 `execution_status`
仅当 `case_status = execution` 时展示与维护。

| 编码 | 中文 label | 颜色 | 可流转至 |
|---|---|---|---|
| `not_applied` | 未申请执行 | 蓝 #3B82F6 | `filing` |
| `filing` | 立案中 | 蓝 #3B82F6 | `executing` |
| `executing` | 执行中 | 金橙 #F59E0B | `recovered`, `execution_ended` |
| `recovered` | 已执行到位 | 绿 #10B981 | `closed` |
| `execution_ended` | 终本 | 灰 #9CA3AF | `resumed` |
| `resumed` | 恢复执行 | 蓝 #3B82F6 | `executing` |

### 三、输出格式要求
1. TypeScript 输出：
   - `CaseStatus` 联合类型（`'pending_contact' | 'consulting' | ...`）。
   - `ExecutionStatus` 联合类型。
   - `CASE_STATUS_META` 常量对象，键为状态编码，值为 `{ label: string; color: string; category: 'flow' | 'result' | 'terminated' | 'abnormal'; next: CaseStatus[] }`。
   - `EXECUTION_STATUS_META` 常量对象，结构同上。
2. 后端输出：
   - Python：`class CaseStatus(str, Enum)` 与 `CASE_STATUS_FLOW: dict[str, list[str]]`。
   - 或直接给出 Java `enum CaseStatus`，含 `label`、`color`、`allowedNext()` 方法。
3. 每个状态注释中文 label 与颜色用途，禁止硬编码颜色值于业务代码。

---

## 提示词 3：前端实现 — 案件状态下拉组件 + 案件管理看板集成

### 目标
实现一个**案件状态下拉组件**（含筛选、分组、二次确认、操作日志），并将其接入“案件管理”页面，按状态分列看板展示案件卡片，支持拖拽改阶段、KPI 展示、回款柱状图与执行进度面板。

---

### 任务 3.1：案件状态下拉组件 `CaseStatusSelect`

#### 功能要求
1. **数据来自后端接口**
   - 组件挂载时调用 `GET /api/meta/case-status` 获取案件主状态枚举。
   - 若案件处于 `execution` 阶段，同时调用 `GET /api/meta/execution-status` 获取执行状态枚举。
2. **分组展示**
   下拉选项按以下顺序分组（带分组标题）：
   - 正常流转：待接洽 → 咨询评估中 → 已咨询待签约 → 已签约待收案 → 立案准备中 → 一审 → 二审 → 执行
   - 结果类：结案 → 调解结案 → 归档
   - 终止类：撤诉 → 驳回 → 终止
   - 异常类：拒接（利益冲突）→ 拒接（费用未达成）
3. **视觉样式**
   - 每个选项左侧带 8px 圆点，颜色取自枚举 `color` 字段。
   - 选中项背景使用该状态色 10% 透明度，文字使用状态色。
   - 当前不可流转的状态置灰并禁用 hover。
4. **筛选过滤**
   - 输入框支持按中文 label 或拼音首字母过滤（如输入“zx”匹配“执行”）。
   - 过滤后仍保持分组结构，空分组隐藏。
5. **二次确认与操作日志**
   - 状态变更时，若目标状态属于结果类/终止类/异常类，弹出确认框要求填写“变更原因”。
   - 确认后调用 `PATCH /api/cases/:id/status`，请求体 `{ status, reason?, execution_status? }`。
   - 后端自动写入 `operation_logs` 表，记录变更人、变更前后状态、原因、时间。
6. **移动端适配**
   - 桌面端：常规下拉浮层。
   - 移动端（屏幕宽度 < 768px）：从底部弹出 ActionSheet 选择层。

#### 接口契约
```http
GET /api/meta/case-status
Response: { ok: true, data: [{ code, label, color, category, next: [code] }] }

GET /api/meta/execution-status
Response: { ok: true, data: [{ code, label, color, next: [code] }] }

PATCH /api/cases/:id/status
Body: { status: CaseStatus, reason?: string, execution_status?: ExecutionStatus }
```

---

### 任务 3.2：案件管理页看板集成

#### 页面布局
- 顶部：5 张 KPI 卡片（同提示词 1）。
- 主体：按案件主状态分列的 Kanban 看板，列顺序与枚举分组一致。
- 右侧固定栏（桌面端）：本月回款进度柱状图 + 执行案件进度列表。
- 移动端：KPI 卡片横向滑动，看板单列垂直滚动，图表折叠在“数据概览”抽屉中。

#### 看板列
每列包含：
- 列头：状态色标签 + 状态中文名 + 案件数量 + 金额合计（万元）。
- 案件卡片：案件名、承办律师头像/首字母、当事人、代理费（万元）、已回款（万元）、回款率。
- 执行状态角标：仅当 `case_status = execution` 时，在卡片右上角显示执行状态小标签（颜色取自 `execution_status.color`）。

#### 过滤功能
1. **状态下拉过滤**：使用任务 3.1 组件，可多选状态，筛选后高亮相关列或隐藏无关列。
2. **执行状态下拉过滤**：单独一个执行状态选择器，仅展示处于 `execution` 的案件。
3. **两下拉组合过滤**：案件状态 + 执行状态组合，结果实时刷新 Kanban。

#### 拖拽改阶段
- 使用原生 HTML5 Drag & Drop 或轻量级拖拽库。
- 拖拽卡片到目标列时，前端先校验目标状态是否在 `next` 允许列表中；非法目标列拒绝放置并抖动提示。
- 放置成功后调用 `PATCH /api/cases/:id/status`，成功后局部刷新该卡片（不整页 reload）。
- 若拖拽导致进入结果类/终止类/异常类，弹出二次确认框填写原因。

#### 金额单位
- 所有金额统一以 **万元** 为单位，保留一位小数（如 `86.5万`）。
- 回款率 = 已回款金额 ÷ 应收金额（代理费），结果保留一位小数；未回款应收用红色，已回款足额用绿色。

#### 图表与执行面板
- **本月回款进度柱状图**：横轴为 1–4 周或按自然周，纵轴万元，深蓝柱体，柱顶标注达成率。
- **执行案件进度面板**：列出各执行状态案件数与金额，带水平进度条；终本案件单独标灰。

#### 操作日志联动
- 任何通过下拉或拖拽触发的状态变更，成功后在前端右下角 toast 提示“状态已更新”。
- 提供“查看变更记录”入口，打开侧滑抽屉展示该案件的 `operation_logs`。

---

### 技术约束
- 框架：单页应用，原生 JS / Vue 3 / React 均可，需说明组件 props 与事件。
- 状态管理：Kanban 数据通过 `GET /api/cases?status=...&execution_status=...` 获取。
- 响应式：支持 1920px / 1440px / 1024px / 768px / 375px 断点。
- 可访问性：下拉支持键盘上下键选择、Enter 确认、Esc 关闭。

### 验收标准
1. 案件状态下拉分组正确、颜色正确、过滤响应灵敏。
2. 非法状态流转被禁用，拖拽到非法列给出明确提示。
3. 状态变更后操作日志正确写入。
4. 看板 KPI、图表、执行面板数据均由接口实时计算，严禁硬编码。
5. 移动端底部弹层正常，无样式错乱。
