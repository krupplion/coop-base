# APP_DATA_NAME · 数据自动备份功能设计方案

版本：v1.0 ｜ 日期：2026-09-01 ｜ 状态：已实现并通过全链路验证

---

## 1. 功能概述

数据自动备份功能用于保障系统数据安全，支持：

- **自定义备份路径**：可把备份文件保存到任意本地目录（如外接硬盘、NAS 映射盘）。
- **灵活的执行周期**：每日定时 / 每周指定星期定时 / 按指定间隔小时数 / 仅手动。
- **可选的备份范围**：数据库、上传文件（发票图片/PDF）、结算单模板、配置文件（AI 服务商配置）四类内容按需勾选。
- **进度反馈与日志**：备份执行时前端实时显示进度文案，结果与历史记录写入 `backup.log` 日志（最近 200 行可在界面查看）。
- **过期备份自动清理**：按保留份数自动删除最旧的备份，节省存储空间。
- **失败可读提示**：权限不足、磁盘满、路径无效、文件占用等 Windows 常见错误自动翻译为中文提示。

---

## 2. 配置项说明

配置持久化于数据库 `settings` 表（键前缀 `backup_`），由系统设置页「数据自动备份」卡片管理。

| 配置项 | 默认值 | 可选值 | 说明 |
| --- | --- | --- | --- |
| `backup_enabled` | `0` | `1` / `0` | 总开关。关闭后定时调度不再触发，但仍可手动立即备份 |
| `backup_path` | 空 | 任意绝对路径 | 备份目录。留空时使用默认目录 `%LOCALAPPDATA%\协同录入系统\backups` |
| `backup_schedule` | `daily` | `daily` / `weekly` / `interval` / `manual` | 执行周期：每日 / 每周 / 按间隔 / 仅手动 |
| `backup_time` | `23:00` | `HH:MM` | `daily` 与 `weekly` 模式的每日触发时刻 |
| `backup_weekday` | `5` | `0`-`6` | `weekly` 模式执行星期（0=周一 … 6=周日，默认周六） |
| `backup_interval_hours` | `24` | 正数 | `interval` 模式间隔小时数（>0） |
| `backup_keep` | `10` | 整数 | 保留备份份数，`0` 表示不自动清理 |
| `backup_scope` | `{"db":1,"uploads":1,"templates":1,"config":1}` | JSON | 备份范围勾选：`db` 数据库 / `uploads` 上传文件 / `templates` 结算单模板 / `config` 配置文件 |

> 保存配置后调度窗口立即重置，新配置即刻生效，无需重启服务。

---

## 3. 备份内容与文件格式

每次备份生成一个 ZIP 文件，命名规则：

```
coop_backup_YYYYMMDD_HHMMSS.zip
```

ZIP 内部结构：

| 包内路径 | 内容 | 受范围开关控制 |
| --- | --- | --- |
| `coop.db` | SQLite 数据库（**在线一致性快照**，见 §6） | `db` |
| `uploads/…` | 上传的发票图片、PDF 等文件（保留相对目录结构） | `uploads` |
| `templates/…` | 结算单 Word 模板 | `templates` |
| `config/ai_providers.json` | AI 服务商配置 | `config` |
| `manifest.json` | 备份元信息：创建时间、触发方式、范围清单、各条目大小 | 始终写入 |

`manifest.json` 示例：

```json
{
  "created_at": "2026-09-01 23:00:01",
  "trigger": "scheduled",
  "db": "coop.db",
  "scope": {"db": 1, "uploads": 1, "templates": 1, "config": 1},
  "items": [
    {"name": "coop.db", "size": 2097152},
    {"name": "uploads", "size": 48234567},
    {"name": "templates", "size": 38194},
    {"name": "config/ai_providers.json", "size": 1024}
  ]
}
```

---

## 4. 执行机制

### 4.1 调度线程

服务启动时挂载一个守护线程，每 60 秒检查一次配置：

```
┌──────────────────────────────────────────────────────────────┐
│ backup_scheduler_loop（守护线程）                              │
│  每 60s：                                                      │
│    enabled==1 且 schedule != manual？                          │
│      └─ 是 → 计算距下次触发秒数 wait                            │
│               wait<=0 → 执行 run_backup_once(trigger=scheduled)│
│                        重置计时并继续                           │
│      └─ 否 → sleep 60s                                         │
└──────────────────────────────────────────────────────────────┘
```

### 4.2 周期模式与下次触发时间计算（`_next_run_seconds`）

| 模式 | 计算规则 |
| --- | --- |
| `daily` | 距今日 `backup_time` 的秒数；已过则顺延至明天同一时刻 |
| `weekly` | 距本周指定 `backup_weekday` 的 `backup_time` 的秒数；已过则顺延至下周 |
| `interval` | 距上次运行（`backup_last_run`）满 `backup_interval_hours` 小时的秒数；从未运行则立即执行 |
| `manual` | 不参与自动调度，仅由用户点击「立即备份」触发 |

### 4.3 并发保护

- 全局 `threading.Lock`（非阻塞获取）：同一时刻仅允许一个备份任务执行；并发请求立即返回「已有备份任务在运行」。
- 立即备份接口异步执行（`threading.Thread`），接口立即返回，前端轮询状态接口获取进度。
- 备份文件先写入 `*.tmp` 临时文件，完成后 `os.replace` 原子改名，避免半截文件。

---

## 5. 进度反馈与日志

### 5.1 进度反馈（前端）

「立即备份」触发后，前端每 1.5s 轮询 `/api/backup/status`：

| 状态字段 | 说明 |
| --- | --- |
| `backup_running` | `1` 表示备份执行中 |
| `backup_progress` | 当前阶段文案（准备清单 → 备份数据库 → 收集上传文件 → 收集结算模板 → 打包压缩） |
| `backup_last_result` | 上次结果 JSON：`{ok, time, file, size, cost, cleaned}` 或 `{ok:false, error}` |

界面以进度条 + 阶段文案实时展示，结束后提示成功（文件名/大小/耗时）或失败原因。

### 5.2 日志

- 写入目录：备份目录下 `backup.log`（UTF-8，追加）；备份目录不可用时自动回退到默认备份目录，保证日志永远可追溯。
- 记录内容：每次执行成功（文件名/大小/耗时/触发方式）与失败（可读原因）、过期清理动作。
- 界面「日志」区域展示最近 200 行。

---

## 6. 一致性设计（数据库在线快照）

为避免备份过程中业务写入导致数据库文件不一致，数据库采用 **SQLite Online Backup API**：

1. 执行 `sqlite3` 的 `src.backup(dst)` 把在线数据库一致性快照到临时文件；
2. 将临时快照写入 ZIP，随后删除临时文件；
3. 业务写入不受备份影响，备份期间系统可正常使用。

---

## 7. 过期备份清理

- 规则：按 `backup_keep` 保留份数清理——备份文件名按时间字典序排序，超出保留份数的最旧文件被删除。
- 触发时机：每次备份成功后自动执行；也可在界面手动触发「立即清理」。
- `backup_keep = 0` 表示不清理。
- 仅清理本系统命名的 `coop_backup_*.zip`，不影响目录内其它文件。

---

## 8. 失败时的可读错误提示

底层 Windows 错误码自动映射为中文提示（`_readable_error`）：

| 底层错误特征 | 界面提示 |
| --- | --- |
| 权限不足（WinError 5 / 13 / 拒绝访问） | 备份目录无写入权限，请检查目录权限设置后重试（阶段：…） |
| 磁盘空间不足（WinError 112 / 28 / no space） | 磁盘空间不足，请清理磁盘空间后重试（阶段：…） |
| 路径无效（WinError 3 / 2 / 系统找不到指定的路径） | 备份路径无效或不存在，请检查备份路径配置（阶段：…） |
| 文件被占用（WinError 32 / 33 / locked） | 文件被占用（可能正在写入），请稍后重试（阶段：…） |
| 其它 | 备份失败：{原始信息}（阶段：…） |

提示中保留「阶段」信息（打包压缩等），便于定位问题环节；失败结果同时写入状态与日志。

---

## 9. 后端 API 接口一览

| 方法 | 路径 | 功能 | 权限 |
| --- | --- | --- | --- |
| GET/PUT | `/api/backup/config` | 读取/保存备份配置 | 系统设置管理 |
| POST | `/api/backup/run` | 立即备份（异步） | 系统设置管理 |
| GET | `/api/backup/status` | 运行状态 + 上次结果（轮询进度） | 系统设置管理 |
| GET | `/api/backup/list` | 备份文件列表（名称/大小/时间） | 系统设置管理 |
| GET | `/api/backup/download/<name>` | 下载备份 ZIP（文件名正则白名单防穿越） | 系统设置管理 |
| DELETE | `/api/backup/delete/<name>` | 删除指定备份 | 系统设置管理 |
| POST | `/api/backup/cleanup` | 手动清理过期备份 | 系统设置管理 |
| GET | `/api/backup/log` | 最近 200 行备份日志 | 系统设置管理 |

> 下载/删除接口对文件名执行 `coop_backup_\d{8}_\d{6}\.zip` 正则白名单校验，杜绝路径穿越。

---

## 10. 前端设置页交互

「系统设置 → 数据自动备份」卡片：

- **启用开关**：自动备份总开关。
- **备份路径**：文本框 + 「使用默认路径」提示（默认目录随界面返回）。
- **执行周期**：下拉框（每日/每周/指定间隔/仅手动），联动显示「时刻」「星期」「间隔小时」输入。
- **备份范围**：四个复选框（数据库 / 上传文件 / 结算模板 / 配置文件）。
- **保留份数**：数字输入（0 = 不清理）。
- **立即备份**：按钮 + 进度条（阶段文案实时刷新）。
- **备份文件列表**：名称/大小/时间 + 下载/删除按钮 + 立即清理。
- **日志区域**：最近 200 行只读展示。

所有操作有成功/失败 toast 提示，保存配置即写库并重置调度窗口。

---

## 11. 验证情况（2026-09-01 已实测通过）

| 验证项 | 结果 |
| --- | --- |
| 后台线程执行完整备份（29.5MB，耗时 3.4s） | ✅ |
| ZIP 校验：db 可打开（案件 12 / 发票 8）、uploads 8 文件、templates 1、manifest 完整 | ✅ |
| 配置保存 / 读取全链路（含保存后立即生效） | ✅ |
| 异步立即备份 + 前端 1.5s 轮询进度 | ✅ |
| 并发保护（第二个请求返回「已有备份任务」） | ✅ |
| 下载 / 删除 / 非法文件名 404 | ✅ |
| 保留份数 keep=1 自动清理 2 个旧备份 | ✅ |
| 4 种周期模式下次触发时间计算 | ✅ |
| 失败路径可读错误提示（路径无效/权限） | ✅ |
| 日志写入与回退目录兜底 | ✅ |
